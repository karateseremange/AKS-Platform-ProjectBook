# ADMIN-006-08 — Cadrage du lot D raccordement privé RECETTE

| Propriété | Valeur |
|---|---|
| **Document ID** | ADMIN-006-08 |
| **Titre** | Raccordement contrôlé du backend privé et recette multi-compte LOG_READ |
| **Version** | 0.16.0 |
| **Statut** | Précontrôle C2 lecture seule autorisé — tests Windows requis |
| **Nature** | Architecture, sécurité, exploitation, tests et retour arrière |
| **Propriétaire** | Product Owner |
| **Dernière mise à jour** | 2026-09-01 |
| **Référence applicative** | Candidate PR #145 : `c39cded9f8d17493780a03cc66e408158ebb5d2d` ; non fusionnée |
| **Dépendances** | ADMIN-006, ADMIN-006-02, ADMIN-006-04, ADMIN-006-06, ADMIN-006-07, ACCESS-002, LOG-001, AUDIT-001 |
| **Environnement** | RECETTE uniquement |

---

## 1. Objet

Ce document cadre le premier raccordement réel de l’architecture privée conçue dans les lots A à C. Il définit les ressources à inventorier, les identités, les droits minimaux, le secret HMAC, le registre anti-rejeu, les preuves, la recette multi-compte et le retour arrière.

Il ne choisit, ne crée et ne modifie aucune ressource Google. Il n’autorise ni secret, ni déploiement, ni partage, ni habilitation ACCESS.

## 2. État de départ historique

État initial conservé pour traçabilité ; l'état courant est au §23. Les éléments suivants étaient intégrés dans `develop` :

- protocole signé `AKS-PRIVATE/1` ;
- commande fermée `LOG_READ_RECENT_V1` ;
- backend privé, adaptateurs et registre anti-rejeu inactifs ;
- client portail contrôlant `LOG_READ` avant tout transport ;
- widget Journaux isolé avec fonctionnement dégradé ;
- 716 tests cumulatifs réussis en RECETTE ;
- restauration exacte du HEAD RECETTE après validation.

Le portail RECETTE connu porte un identifiant Apps Script suffixé `eIRxs4`. Aucun projet backend, endpoint, secret ou support LOG n’est désigné par le présent document.

## 3. Objectif fonctionnel

Un compte secondaire disposant de `LOG_READ` doit pouvoir :

1. ouvrir le portail RECETTE sous sa propre identité ;
2. voir la destination Journaux sans acquérir de capacité `CONFIG_*` ;
3. déclencher une lecture signée côté serveur ;
4. recevoir uniquement des événements minimisés ;
5. conserver l’accès au reste du portail si le backend est indisponible.

Après retrait de `LOG_READ`, la destination doit disparaître et tout appel direct doit être refusé avant transport.

## 4. Architecture cible

| Composant | Identité d’exécution | Responsabilité |
|---|---|---|
| Portail RECETTE | utilisateur accédant | identité utilisateur, contrôle ACCESS, interface |
| Client privé du portail | contexte serveur du portail | construction et signature de la requête |
| Endpoint backend RECETTE | identité technique dédiée | validation, anti-rejeu et lecture LOG |
| Registre anti-rejeu | propriété du backend | consommation atomique des requêtes et nonces |
| Support LOG RECETTE | accès technique minimal | source des événements minimisés |
| Support de preuve | identité technique | trace minimale corrélée du traitement |

Le navigateur ne reçoit jamais l’URL privée, le secret HMAC, l’identité technique ni les identifiants Google des supports.

## 5. Décisions à prendre après inventaire

Le précontrôle doit permettre de décider explicitement :

- réutilisation ou création d’un projet backend distinct ;
- compte technique propriétaire et opérateurs autorisés ;
- mode de déploiement du backend Apps Script ;
- niveau d’accès du Web App compatible avec l’appel serveur-à-serveur ;
- support LOG RECETTE exact ;
- support de preuve exact ;
- propriétés serveur à installer ;
- stratégie de rotation du secret ;
- durée et méthode de nettoyage du registre anti-rejeu.

Aucune valeur ne doit être déduite d’un suffixe, d’un nom proche ou d’un ancien déploiement.

## 6. Inventaire D0 — lecture seule

Le premier sous-lot opérationnel doit rester strictement en lecture seule.

### 6.1 Portail RECETTE

Relever et figer :

- identifiant Apps Script complet ;
- HEAD et nombre de fichiers ;
- manifeste ;
- déploiements, versions et URL ;
- mode `executeAs` et audience ;
- propriétés existantes, uniquement par noms et empreintes ;
- propriétaires et éditeurs ;
- état exact du coupe-circuit privé.

### 6.2 Candidats backend

Pour chaque projet candidat :

- identifiant complet ;
- propriétaire ;
- éditeurs ;
- environnement déclaré ;
- HEAD, versions et déploiements ;
- manifeste et services avancés ;
- propriétés existantes par noms et empreintes ;
- présence éventuelle d’un `doPost` ;
- absence de dépendance PRODUCTION.

Si aucun candidat conforme n’existe, le précontrôle doit le déclarer sans créer de projet.

### 6.3 Supports LOG et preuve

Pour chaque support candidat :

- identifiant complet ;
- nom exact ;
- propriétaire ;
- partages ;
- onglets et en-têtes ;
- environnement déclaré ;
- volume et politique de conservation ;
- compatibilité avec les contrats LOG et AUDIT ;
- absence de données PRODUCTION.

L’inventaire n’autorise aucune lecture de contenu métier au-delà des métadonnées strictement nécessaires.

## 7. Identités et droits minimaux

### 7.1 Utilisateur du portail

L’adresse est résolue côté serveur par le portail. Elle n’est jamais acceptée depuis le navigateur.

L’utilisateur reçoit seulement les capacités fonctionnelles nécessaires. `LOG_READ` ne confère ni `CONFIG_READ`, ni `CONFIG_WRITE`, ni `CONFIG_RESET`, ni `ACCESS_MANAGE`.

### 7.2 Identité technique backend

Exigences cibles initiales. La décision RECETTE ultérieure d'ADMIN-006-14 §3 utilise le propriétaire existant, sans compte technique distinct ; ces exigences ne constituent pas une attestation de ses droits réels.

L’identité technique doit :

- être distincte des comptes de test utilisateur ;
- avoir uniquement la lecture du support LOG RECETTE ;
- avoir uniquement l’écriture minimale du support de preuve retenu ;
- ne disposer d’aucun accès aux supports PRODUCTION ;
- ne pas être utilisée pour naviguer dans le portail ;
- être inventoriée avant tout partage.

### 7.3 Opérateur

L’opérateur autorisant le raccordement doit être vérifié séparément. Son rôle ne doit pas être confondu avec l’identité technique d’exécution.

## 8. Déploiement privé

Le backend doit résider dans un projet distinct du portail `USER_ACCESSING`.

Avant création d’un déploiement, le protocole doit fixer :

- l’identité d’exécution ;
- l’audience minimale techniquement viable ;
- la version exacte ;
- la description de version ;
- l’URL attendue ;
- le mécanisme d’authentification applicative HMAC ;
- l’absence de route autre que la commande fermée ;
- la procédure de suppression ou de désactivation.

Une audience réseau large ne vaut jamais autorisation fonctionnelle. Toute requête reste refusée sans signature, environnement, projet appelant, fenêtre temporelle et anti-rejeu valides.

## 9. Secret HMAC

### 9.1 Génération

Le secret doit :

- être généré par une source cryptographique ;
- contenir au moins 256 bits d’entropie ;
- ne jamais transiter dans le chat, Git, un rapport ou une cellule ;
- être installé directement dans les propriétés serveur autorisées ;
- être référencé par empreinte uniquement dans les preuves.

### 9.2 Stockage

Les propriétés logiques sont :

- `AKS_PRIVATE_HMAC_CURRENT` ;
- `AKS_PRIVATE_HMAC_PREVIOUS` pendant une rotation bornée ;
- `AKS_PRIVATE_SECRET_VERSION`.

Le portail et le backend doivent recevoir la même version courante. Toute divergence maintient Journaux indisponible sans bloquer Administration.

### 9.3 Rotation

La rotation suit quatre phases :

1. installer le nouveau secret comme courant côté backend et conserver l’ancien ;
2. installer le nouveau secret côté portail ;
3. vérifier le nouveau flux ;
4. supprimer l’ancien secret des deux côtés après la fenêtre autorisée.

Un retour arrière doit être possible tant que l’ancien secret est conservé. Les valeurs ne sont jamais affichées.

## 10. Configuration serveur

### 10.1 Portail

Les propriétés futures minimales sont :

- activation explicite ;
- environnement exact `RECETTE` ;
- URL backend ;
- projet appelant logique ;
- secret courant et version ;
- délai borné.

### 10.2 Backend

Les propriétés futures minimales sont :

- activation explicite ;
- environnement exact `RECETTE` ;
- projet appelant autorisé ;
- secrets courant et précédent ;
- identifiants des supports LOG et preuve ;
- version technique ;
- paramètres anti-rejeu bornés.

Toute propriété absente, ambiguë ou incohérente maintient le composant fermé.

## 11. Registre anti-rejeu

L’activation future utilise exclusivement les adaptateurs déjà intégrés :

- `ScriptProperties` du projet backend ;
- `ScriptLock` ;
- empreintes séparées du `requestId` et du nonce ;
- écriture atomique ;
- relecture obligatoire ;
- expiration bornée ;
- purge bornée ;
- libération du verrou dans `finally`.

Le précontrôle doit sauvegarder les propriétés préexistantes et refuser toute collision de préfixe. Le retour arrière supprime seulement les clés créées par l’opération autorisée.

## 12. Preuve technique

Chaque appel accepté produit une preuve minimisée :

- environnement ;
- commande ;
- empreinte de l’acteur ;
- identifiants de requête et de corrélation ;
- résultat ;
- durée ;
- nombre d’événements retournés ;
- version backend.

Sont interdits :

- adresse utilisateur en clair ;
- secret ou signature ;
- URL backend ;
- corps complet de requête ou réponse ;
- contenu détaillé des journaux ;
- identifiant Google inutile.

La preuve d’un refus ne doit pas révéler la cause cryptographique précise à l’appelant.

## 13. Séquence opérationnelle fermée

### D0 — inventaire

Lecture seule des projets, déploiements, propriétés par empreintes, supports et partages.

### D1 — préparation des ressources

Création ou choix explicite du backend et des supports, sous autorisation distincte. Aucun secret ni déploiement.

### D2 — installation du secret

Sauvegarde des propriétés, génération hors journal, installation croisée, vérification par empreintes. Transport encore désactivé.

### D3 — déploiement backend

Push contrôlé, relecture, tests, création de version et déploiement privé. Portail encore désactivé.

### D4 — raccordement portail

Plan détaillé au §20 : implémentation inactive et validation réversible préalables, puis raccordement technique sous autorisation distincte.

### D5 — recette multi-compte

Attribution temporaire et séparée de `LOG_READ`, tests fonctionnels, retrait du droit et contrôle des preuves.

### D6 — clôture ou retour arrière

Conservation autorisée ou suppression des propriétés, déploiement, partages et habilitations temporaires.

Chaque sous-lot exige une autorisation explicite et un rapport avant le suivant.

## 14. Recette multi-compte

Au minimum, tester :

| Scénario | Résultat attendu |
|---|---|
| compte principal avec `LOG_READ` | Journaux disponible |
| compte secondaire avec `LOG_READ` | Journaux disponible |
| compte secondaire sans `LOG_READ` | destination absente et appel refusé avant transport |
| `LOG_READ` sans `CONFIG_*` | Journaux disponible, Paramétrage absent |
| `CONFIG_READ` sans `LOG_READ` | Paramétrage disponible, Journaux absent |
| backend désactivé | Administration disponible, widget dégradé |
| secret incohérent | Administration disponible, aucune donnée affichée |
| requête rejouée | refus anti-rejeu |
| retrait de `LOG_READ` | effet immédiat sans réutilisation d’autorisation |
| réessai | nouvelle requête et nouveau nonce |

Les comptes de test et la durée de l’habilitation temporaire doivent être approuvés avant écriture ACCESS.

## 15. Tests techniques

La validation doit inclure :

- suite cumulative complète ;
- refus avant transport ;
- projet appelant erroné ;
- environnement autre que `RECETTE` ;
- signature altérée ;
- requête expirée ;
- rejeu du `requestId` ;
- rejeu du nonce ;
- verrou indisponible ;
- support LOG indisponible ;
- preuve indisponible ;
- réponse altérée ou non corrélée ;
- rotation courant/précédent ;
- absence de fuite dans les journaux ;
- fonctionnement dégradé du portail ;
- restauration après chaque panne injectée.

## 16. Barrières de sécurité

L’opération doit s’arrêter si :

- le projet portail n’est pas exactement celui attendu ;
- une ressource contient un marqueur PRODUCTION ;
- le propriétaire ou le partage est inattendu ;
- le support LOG ou preuve n’est pas identifiable sans ambiguïté ;
- un secret existe avec une empreinte inconnue ;
- une propriété serait écrasée ;
- le backend expose une route non prévue ;
- la suite cumulative échoue ;
- la restauration ne peut pas être prouvée ;
- une habilitation temporaire ne peut pas être retirée.

## 17. Retour arrière

Procédure générale initiale ; pour D4, appliquer le §20.7 afin de préserver les ressources D2/D3. Le retour arrière complet, uniquement si son périmètre est autorisé, suit cet ordre :

1. désactiver le client du portail ;
2. vérifier Administration en mode dégradé ;
3. retirer l’URL backend du portail ;
4. restaurer les propriétés du portail ;
5. désactiver le backend ;
6. restaurer les propriétés du backend ;
7. supprimer uniquement les clés anti-rejeu créées ;
8. retirer les partages temporaires ;
9. retirer les habilitations ACCESS temporaires ;
10. remettre le déploiement backend à son état initial ou le supprimer s’il a été créé pour la recette ;
11. relire intégralement les deux projets ;
12. vérifier les supports et preuves ;
13. archiver les rapports sans secret.

Toute restauration partielle est un échec.

## 18. Rapports et preuves de contrôle

Chaque sous-lot doit produire :

- état avant et après ;
- identifiants et versions non secrets ;
- inventaire des mutations autorisées ;
- empreintes des sauvegardes ;
- résultats des tests ;
- état des déploiements ;
- état des propriétés par noms et empreintes ;
- état des partages ;
- état des habilitations temporaires ;
- résultat du retour arrière.

Les rapports ne contiennent aucune valeur secrète, signature ou URL sensible complète.

## 19. Critères d’acceptation du cadrage

Le cadrage est recevable si :

- l’architecture sépare portail utilisateur et backend technique ;
- `LOG_READ` est contrôlé avant transport ;
- Paramétrage et Journaux restent indépendants ;
- les ressources sont inventoriées avant création ;
- le secret est installé sans exposition ;
- le registre anti-rejeu est borné et restaurable ;
- la preuve est minimisée ;
- la recette multi-compte couvre attribution et retrait ;
- le fonctionnement dégradé est testé ;
- chaque étape possède son autorisation et son retour arrière ;
- aucune opération PRODUCTION ou INSCRIPTIONS-011 n’est engagée.

## 20. D4 — plan détaillé proposé

### 20.1 État et provenance

Précontrôle GitHub du 1er septembre 2026 : application `064ca709a754915a87451e829c24088ec878fdde`, Project Book `9b1bdf2ba318a6e1913d3d953f8c68949934b6a5`. Aucune interrogation Google. Les preuves opérationnelles sont celles transmises par le Product Owner et consignées dans [ADMIN-006-14](ADMIN-006-14.md), §14.

D3-D4 a validé signature, preuve et refus de rejeu, puis restauration du code et des propriétés. Le backend version 1 reste inactif. Le test éditeur ne prouve pas le parcours navigateur USER_ACCESSING ni les habilitations réelles. Le HEAD portail restauré à 261 fichiers ne correspond pas au code applicatif courant : ajouter seulement une URL ne suffit pas.

Ce plan a été intégré par la PR documentaire #224 au commit `eafadcb02d61236d521666e968b5d873d9c0c0a6`, après autorisation explicite de fusion. Le Product Owner a ensuite autorisé D4-A applicatif ; son résultat figure au §21. Aucune opération Google n'est autorisée par ce plan. INSCRIPTIONS-011 reste non engagé.

### 20.2 Précontrôle du code

Sources sous `src/`, au commit applicatif figé ci-dessus :

| Source | Constat | Travail proposé |
|---|---|---|
| `core/private/PrivatePortalLogClient.gs` | Factory de production encore inerte | Runtime RECETTE gardé, configuration, crypto, identifiants et transport injectables |
| `app/AdminDashboardController.gs` | Modèle Journaux construit synchroniquement | Coque Administration rendue avant tout appel réseau Journaux |
| `app/WebApp.gs` et `app/AdminLogController.gs` | Route logs utilisant encore AKS.LogEventRepository et limites 25/50/100 | Vue privée RECETTE utilisant le même client signé que le widget |
| `core/access/AccessPortalProjectionService.gs` | Destinations séparées, exception historique bootstrap | Autorisation serveur indépendante de la visibilité ; tester bootstrap |
| `core/access/AccessService.gs` | Identité et capacités avec dépendances de configuration | Vérifier tout le chemin sous identité utilisateur, sans partager LOG pour contourner un échec ACCESS |
| `core/private/PrivateBackendRuntime.gs` | Regex curseur rejetant notamment row:10 | Pagination exclue du prototype ; correction backend distincte avant extension |

Le constat sur les curseurs est statique, pas un test Google. Aucun backend publié n'est modifié dans D4.

### 20.3 Contrat fonctionnel et sécurité

Widget : cinq événements récents ; page Journaux RECETTE : vingt maximum et filtre de sévérité. Pas de pagination, recherche libre, catégorie ou export dans ce prototype. Ne pas présenter cette vue comme l'équivalent complet des filtres historiques.

La recherche porte sur au plus les 500 dernières lignes de données du journal, hors en-tête, selon leur position dans la feuille. Le backend sélectionne d'abord cette fenêtre, applique ensuite le filtre de sévérité éventuel, puis limite les résultats aux cinq événements du widget ou aux vingt événements maximum de la page, en parcourant les lignes de la plus récente à la plus ancienne. Il n'explore pas les lignes antérieures à cette fenêtre pour compléter les résultats. Cette consultation ne garantit donc aucune exhaustivité historique ; un résultat vide ne démontre pas l'absence d'événements correspondants plus anciens.

L'interface doit afficher la limite à proximité des résultats, y compris lorsqu'ils sont vides : « Consultation limitée aux 500 dernières lignes du journal ; historique non exhaustif. » En cas de résultat vide, afficher « Aucun événement correspondant dans la fenêtre consultée (au plus 500 dernières lignes). » Ne pas présenter cet état comme l'absence d'événements dans tout le journal.

Cette précision décrit le comportement du backend au commit applicatif indiqué au §20.1. Elle ne modifie ni le backend, ni l'architecture, ni la validation technique D3-D4.

DTO fermé : date, sévérité, code, message borné, corrélation. Affichage texte échappé, sans actor/context. Un message peut néanmoins contenir des données personnelles : utiliser des événements synthétiques pour la preuve initiale.

Chaque RPC et chaque réessai contrôle LOG_READ côté serveur avant lecture du secret, signature et transport. Identité vide ou ACCESS indisponible : refus fermé. Ne jamais accepter acteur, projet, endpoint, secret ou support depuis le navigateur. Tester explicitement les exceptions bootstrap, sans les assimiler à une habilitation attribuée.

Aucun repli vers le dépôt LOG Google direct dans le chemin privé RECETTE, même en cas d'erreur ou de désactivation. Préserver les comportements hors RECETTE. CONFIG reste hors prototype ; LOG_READ ne confère aucune capacité CONFIG ou ACCESS_MANAGE.

### 20.4 Configuration et transport

Propriétés portail proposées selon [ADMIN-006-06](ADMIN-006-06.md) :

| Propriété | Contrat |
|---|---|
| AKS_PRIVATE_PORTAL_ENABLED | Absente ou false : inactif ; true uniquement pendant une fenêtre autorisée |
| AKS_PRIVATE_PORTAL_ENVIRONMENT | RECETTE exact |
| AKS_PRIVATE_BACKEND_URL | Endpoint /exec exact du rapport local protégé D3-D3, jamais dans Git ou le navigateur |
| AKS_PRIVATE_CALLER_PROJECT | Valeur exacte attendue par le backend, contrôlée avec le Script ID réel du portail |
| AKS_PRIVATE_HMAC_CURRENT et AKS_PRIVATE_SECRET_VERSION | Valeurs D2-B préservées, jamais affichées |
| AKS_PRIVATE_HMAC_PREVIOUS | Reste absente ; rotation exclue |
| AKS_PRIVATE_TIMEOUT_MS | Budget logique proposé 10 000 ms, pas un timeout natif UrlFetchApp |

Comparer le Script ID réel à la cible RECETTE complète approuvée ; ni suffixe seul ni environnement autodéclaré ne suffisent. Aucun I/O au chargement. Ne pas installer les clés PORTAL dans le backend, dont la configuration est fermée.

Réutiliser AKS-PRIVATE/1 et la validation des réponses signées : environnement, requestId, correlationId, empreinte et HMAC. POST HTTPS JSON vers l'endpoint exact, certificat validé, sans cookie ou jeton OAuth utilisateur. Tester explicitement les redirections ContentService : récupération GET de réponse vers destination Google strictement contrôlée, sans retransmettre le corps signé à une autre origine. Refuser les destinations arbitraires.

Corps signé au plus 16 Kio, réponse au plus 32 Kio avant parsing, vingt événements maximum, fenêtre protocolaire de 60 secondes. La limite de réponse est un contrôle après réception, pas une garantie de téléchargement streaming borné. Identifiants distincts ; nonce avec au moins 128 bits d'entropie démontrée, sans assimiler un UUIDv4 unique à 128 bits aléatoires.

Pas de paramètre UrlFetchApp fictif timeout. RPC asynchrone après affichage du portail ; état indisponible au dépassement du budget UX, rejet des réponses tardives, double-clic maîtrisé, aucun retry automatique. Le budget UI n'annule pas le fetch serveur ; une preuve peut être écrite après abandon de l'attente par l'interface.

### 20.5 Séquence et autorisations futures

| Étape | Livrable | Limite |
|---|---|---|
| D4-A | Branche applicative : runtime inactif, RPC sécurisé, widget et page Journaux, tests complets, PR develop | Aucune opération Google |
| D4-B | Validation réversible de la candidate figée : sauvegarde exacte, push temporaire, relecture, suite cumulative, restauration | Aucune activation, version ou déploiement |
| D4-C | Précontrôle Google autorisé, configuration et raccordement technique navigateur USER_ACCESSING, fenêtre supervisée | Mutations et cible de déploiement autorisées explicitement |
| D5 | Matrice multi-compte §14, attribution et retrait LOG_READ, preuves corrélées | Écritures ACCESS séparément autorisées, jamais implicites dans D4 |

Avant D4-C, relever l'état réel des identifiants, propriétaires/éditeurs, audiences, identités, manifestes, code, propriétés par noms/empreintes et inventaires. Aucun numéro de version n'est déduit d'un ancien inventaire. La décision d'identité RECETTE reste celle d'ADMIN-006-14 §3 : propriétaire existant, sans compte technique distinct. Aucune absence d'accès production de ce propriétaire n'est nouvellement attestée.

Construire le package portail depuis une tête revue exacte ; expliquer tous ses écarts avec la sauvegarde avant push. Ne jamais copier le manifeste backend USER_DEPLOYING/ANYONE_ANONYMOUS vers le portail. Vérifier scopes dont external_request et consentement utilisateur avant preuve navigateur. Tout changement de manifeste ou déploiement doit être expressément inclus, pas improvisé localement.

Activer backend puis portail uniquement dans une fenêtre supervisée. Fin de campagne : fermer les deux coupe-circuits et vérifier. Aucun maintien actif implicite ; opérateur de secours et procédure de fermeture requis. Ne pas promettre une désactivation automatique en cas d'interruption du poste.

### 20.6 Tests à implémenter

| Famille | Résultats attendus |
|---|---|
| Autorisation | Identité vide, sans LOG_READ, ACCESS indisponible, appel direct : zéro signature et transport |
| Garde | Mauvais projet, environnement, endpoint, flag absent/false : refus fermé |
| Données | Limites 5/20, sévérité, refus curseur/champs réservés, échappement XSS, aucun secret/endpoint dans DTO ou logs |
| Fenêtre de recherche | Jeux de 499, 500 et 501 lignes de données : en-tête exclu, dernière ligne et borne ancienne incluses ; avec 501 lignes, événement correspondant immédiatement hors fenêtre exclu, même si le résultat est vide ; aucune recherche antérieure pour compléter |
| Ordre du filtrage | Dans la fenêtre, placer des événements de la sévérité demandée au-delà des cinq ou vingt premières lignes : filtrage avant limitation à 5/20, ordre des résultats conservé |
| Information utilisateur | Mention de la fenêtre et de l'absence d'exhaustivité affichée avec ou sans résultats ; résultat vide limité explicitement à la fenêtre consultée |
| Transport | Signature valide/altérée, mauvaise corrélation, HTTP erreur, HTML, JSON trop gros, redirection interdite |
| Résilience | Lenteur, exception, réponse tardive, double-clic : portail utilisable sans repli Sheets |
| Non-régression | Analytics, Mes accès, comptes, questionnaire et navigation CONFIG hors périmètre préservés |
| Anti-rejeu | Tests injectés de rejeu/concurrence ; réel seulement sous autorisation de campagne |
| D5 | LOG_READ seul sans CONFIG ni partage LOG, compte refusé, retrait puis nouvel appel refusé |

Recalculer le total cumulatif depuis la candidate et l'inventaire des tests uniques. Ne pas imposer un total historique. Séparer tests locaux simulés, Apps Script et navigateur. Aucun test D4 n'est déclaré exécuté par ce cadrage.

### 20.7 Retour arrière D4

Cette spécialisation du §17 préserve les ressources déjà établies :

1. fermer portail et backend, vérifier les flags, sans prétendre annuler une requête en vol ;
2. restaurer seulement les propriétés non secrètes changées par D4, dont l'absence initiale de l'URL ; préserver HMAC et version D2-B ;
3. restaurer le HEAD portail et son manifeste exactement ; retirer seulement les fichiers supplémentaires identifiés puis relire — un push réussi ne suffit pas ;
4. si autorisé et modifié, réassocier uniquement le déploiement portail cible à sa version initiale ; comparer la liste complète ; consigner les nouvelles versions immuables sans tenter de les supprimer ;
5. conserver backend version 1, déploiement et propriétés préexistantes ;
6. conserver les preuves ; ne pas effacer de clé anti-rejeu avant expiration de sa fenêtre d'acceptation ; tout nettoyage ultérieur est borné aux clés du run, sous verrou, sans suppression globale ;
7. comparer code, manifestes, propriétés par empreintes et inventaires. Toute restauration partielle est un échec.

Aucun partage ou retrait/attribution ACCESS n'est inclus. Rapports sans secret, signature, corps signé, URL backend ni contenu LOG ; références opérationnelles sensibles locales et protégées.

### 20.8 Décision suivante

D4-A est proposé dans la PR applicative #145 (§21), revue au §22 avec le protocole D4-B. B0 et l'autorisation B1 initiaux sont conservés aux §24–25. B1 a échoué sur deux tests, puis le HEAD a été restauré exactement selon le rapport opérateur (§26). La nouvelle candidate `c39cded9f8d17493780a03cc66e408158ebb5d2d` ne corrige que les tests et le harnais. L'outillage C2-local-r1 est prêt pour reconstruire localement son package à partir des preuves sauvegardées (§27). Une revalidation distante et une nouvelle autorisation sont requises avant tout B1 ; aucune lecture Google nouvelle, écriture ou fusion n'est autorisée par cette préparation. ADMIN-006 n'est pas résolu et les autres modules ne sont pas engagés.

## 21. Résultat D4-A — implémentation inactive et validation locale

### 21.1 Autorisation et références figées

Le Product Owner a autorisé l'implémentation inactive, les tests locaux et l'ouverture des PR vers `develop` par « suivant », après intégration du plan #224. Cette autorisation ne couvre aucune fusion supplémentaire ni opération Google.

| Référence | Valeur |
|---|---|
| Base applicative | `064ca709a754915a87451e829c24088ec878fdde` |
| Candidate applicative | `688c81bb64e6aa09f9955743b783fca989369ae2` |
| Branche et PR | `feature/admin-006-d4a-portal` — [AKS-Platform #145](https://github.com/karateseremange/AKS-Platform/pull/145), ouverte vers `develop`, non fusionnée |
| Base documentaire | `eafadcb02d61236d521666e968b5d873d9c0c0a6`, fusion de [Project Book #224](https://github.com/karateseremange/AKS-Platform-ProjectBook/pull/224) |
| Périmètre applicatif | 14 fichiers : runtime, RPC, intégration des vues, tests et rapports locaux |

### 21.2 Comportement livré et choix d'implémentation

- Le runtime compare le Script ID réel au portail RECETTE complet avant ACCESS ou propriétés. Il n'effectue aucun I/O au chargement de son fichier. Le flag absent ou `false` laisse le transport inactif.
- L'identité serveur, `LOG_READ` et l'attribution effective ADMINISTRATION concordante sont exigées avant le secret, puis contrôlées avant signature et transport. Un snapshot `bootstrap` est refusé : sa visibilité historique n'autorise pas une lecture privée. Les appels directs et les réessais repassent les contrôles.
- Les propriétés sont lues individuellement après autorisation. L'environnement et le projet appelant doivent correspondre exactement ; la clé précédente reste absente. La configuration accepte un endpoint HTTPS `script.google.com/macros/s/.../exec`, conservé côté serveur. Sa concordance avec l'endpoint protégé du rapport D3-D3 reste à vérifier sous autorisation D4-C ; aucune valeur réelle n'a été consultée ou inscrite dans Git.
- Le protocole et le client existants sont réutilisés. Chaque enveloppe utilise des identifiants UUIDv4 distincts et un nonce formé de deux UUIDv4 frais sans tirets. Chaque UUIDv4 réserve six bits et possède 122 positions aléatoires : la construction vise 244 bits sous le contrat du générateur, pas une garantie absolue d'unicité. [Utilities.getUuid](https://developers.google.com/apps-script/reference/utilities/utilities#getuuid) est documenté comme équivalent à [UUID.randomUUID](https://docs.oracle.com/javase/7/docs/api/java/util/UUID.html#randomUUID()), générateur pseudo-aléatoire cryptographiquement fort. La version, la variante et les répétitions internes sont contrôlées ; les tests locaux ne mesurent pas l'entropie de Google.
- Le POST JSON valide le certificat et désactive les redirections automatiques. Une seule redirection 302/303 est admise vers `https://script.googleusercontent.com/macros/echo`, avec paramètres autorisés contrôlés, puis GET sans corps signé. Les autres destinations, répétitions de paramètres et secondes redirections sont refusées. Aucun cookie ni jeton OAuth utilisateur n'est transmis.
- Les plafonds UTF-8 sont de 16 Kio pour la requête et 32 Kio pour la réponse, cette dernière étant contrôlée après réception et avant parsing. HTTP 200 et type JSON sont requis. Le DTO navigateur reste fermé : état, disponibilité, événements minimisés ; aucun curseur, acteur ou contexte n'est exposé.
- Le widget fixe cinq résultats ; la page fixe vingt résultats avec une sévérité optionnelle. La RPC refuse notamment acteur, endpoint, curseur et limite fournis par le navigateur. L'interface rappelle les 500 dernières lignes et l'absence d'exhaustivité historique, également à vide. Aucun changement de la fenêtre ou de l'ordre filtrage puis limitation n'est apporté au backend.
- En RECETTE, les deux vues rendent leur coque avant la RPC et n'utilisent aucun repli vers le dépôt LOG direct. Les autres projets conservent le comportement existant. Les données dynamiques sont rendues avec `textContent`.
- Le budget UX est fixé à 10 000 ms. `AKS_PRIVATE_TIMEOUT_MS` est optionnelle ; si présente, elle doit valoir exactement `10000`, afin de rester cohérente avec l'interface. Il n'existe aucun timeout natif UrlFetchApp ajouté. Un dépassement écarte la réponse tardive ; le réessai reste bloqué tant que l'appel serveur précédent n'a pas rendu la main. Aucun retry automatique ; chaque réessai explicite produit une nouvelle requête.

Le backend, le protocole, l'anti-rejeu, le package backend et les manifestes restent identiques à la base. L'architecture et la validation D3-D4 ne sont pas remises en cause.

### 21.3 Validation locale reproductible

| Vérification | Résultat et portée |
|---|---|
| Base avec le même harnais Node | 729/729 |
| Candidate, suite cumulative | **761/761 tests uniques**, dont **32 tests D4-A** ; inventaire sans doublon |
| Client avec DOM et horloge simulés | **8/8**, distincts de la suite cumulative |
| Diff | `git diff --check` conforme ; absence de changement backend/protocole/anti-rejeu/manifestes ; 14 fichiers relus sur GitHub et identiques à la candidate locale |
| Apps Script réel | Non exécuté ; D4-B requis |
| Navigateur Google USER_ACCESSING et multi-compte | Non exécuté ; D4-C puis D5 requis |

La suite locale utilise Node `v24.19.0`, des services injectés et des adaptateurs en mémoire. Elle n'accède ni à Sheets, ni à Drive, ni au réseau Google. L'émulation de templates et de DOM ne valide pas le moteur HTML Apps Script, le consentement OAuth ou un navigateur réel. Des corrections locales du harnais et du schéma de fixture ACCESS ont précédé le résultat final ; elles ne constituent pas des incidents Google.

Depuis la racine de la candidate, sans dépendance externe :

```text
node tools/run-local-tests.js
node --test tools/private-logs-client.test.js
```

Les tests D4-A couvrent les gardes et l'ordre des contrôles, ACCESS indisponible/inactif/expiré, le bootstrap refusé, la configuration fermée, les identifiants, les réponses et redirections, les bornes UTF-8, le DTO, les routes RECETTE et le maintien du comportement ailleurs. Les cas 499/500/501 lignes couvrent les limites de fenêtre et un événement correspondant immédiatement hors fenêtre donnant un résultat vide. Les cas de sévérité vérifient le filtrage avant les limites 5/20. Le client couvre double-clic, attente, réponse tardive, erreur générique, filtre, résultat vide et affichage texte. Les tests existants de signature, rejeu et concurrence sont conservés dans la suite cumulative.

Rapports au commit candidat immuable :

- [Suite cumulative locale](https://github.com/karateseremange/AKS-Platform/blob/688c81bb64e6aa09f9955743b783fca989369ae2/docs/validation/admin006-d4a-local.json), SHA-256 `ec44906c968d77296adf3e11612eb48e8f2145f9b8d8ca24185b42d276ed1751`.
- [Client DOM simulé](https://github.com/karateseremange/AKS-Platform/blob/688c81bb64e6aa09f9955743b783fca989369ae2/docs/validation/admin006-d4a-client.tap), SHA-256 `ee402a9dc69b900cac5c9244cc0f4a705d2351ba7f8482e55cfaabe57f640950`.

Le rapport cumulatif du `2026-09-01T10:52:21.538Z` porte l'empreinte des sources `02b38ac172087275c40ea1753f47af551970e1300b4a30c188a1bae39df8cda0`. Le harnais calcule cette empreinte sur les chemins relatifs triés et les octets des fichiers `.gs`, `.html` et `.json` sous `src`, séparés par des octets NUL. Ce résultat ne vaut pas preuve d'installation dans Apps Script.

### 21.4 État opérationnel et suites

Aucun push Google, activation, version, déploiement, propriété, secret, partage ou attribution ACCESS n'a été effectué dans D4-A. Aucune restauration Google n'était donc nécessaire. Le dernier état opérationnel reste celui rapporté en D3-D4 dans ADMIN-006-14 §14 ; il n'a pas été réobservé. Les deux PR de D4-A sont proposées pour revue, sans fusion.

Après revue de la candidate figée, préparer D4-B : sauvegarde exacte du portail, explication des écarts, push temporaire, relecture, suite cumulative Apps Script et restauration exacte, sous autorisation distincte et sans activation ni déploiement. Le manifeste demeure celui du portail ; les scopes effectivement inférés, notamment `external_request`, et le consentement USER_ACCESSING devront être vérifiés avant la preuve navigateur D4-C. Aucun ajustement de manifeste ou déploiement n'est implicitement autorisé.

D4-C doit encore établir le raccordement navigateur réel ; D5 doit démontrer LOG_READ seul, refus et retrait d'habilitation avec les comptes autorisés. Le défaut de curseur reste hors prototype, sans modification backend. ADMIN-006 demeure bloquant ; INSCRIPTIONS-011 reste non engagé.

## 22. Revue D4-A et protocole D4-B réversible

### 22.1 Résultat de revue du 1er septembre 2026

Autorisation reçue : « Suivant » après proposition de revue conjointe des PR et préparation du protocole D4-B, sans fusion ni exécution Google.

| Élément revu | Référence et conclusion |
|---|---|
| PR applicative #145 | Tête `688c81bb64e6aa09f9955743b783fca989369ae2`, base `064ca709a754915a87451e829c24088ec878fdde`, ouverte, non fusionnée ; 14 fichiers distants conformes aux fichiers examinés |
| PR documentaire #225 | Tête initiale revue `4e7ebf93bbf184d0511ff3eea0aedff80ced7da9`, base `eafadcb02d61236d521666e968b5d873d9c0c0a6` ; trois documents concordants ; présente mise à jour sur la même PR |
| Contrat et contrôles | Gardes RECETTE/ACCESS avant secret, bootstrap refusé, vérifications avant signature/transport, données et RPC fermées, redirections contrôlées, aucune lecture LOG directe dans le parcours privé |
| Interface et fenêtre | Coque asynchrone, borne des 500 lignes explicite y compris à vide, tests hors fenêtre et filtrage avant limites 5/20, texte dynamique échappé |
| Portée de validation | Aucun défaut bloquant identifié par cette revue statique et locale ; validation Apps Script, consentement et navigateur toujours requis |

Deux vérifications locales ciblées ont levé les incertitudes de préparation : la suite a été rejouée avec le Script ID RECETTE simulé (**761/761**), puis avec le chargement des `.gs` et des six `.js` serveur (**761/761**, 242 fichiers serveur). Les services restent simulés, sans accès Google. Ces contrôles ne changent ni la candidate ni les rapports D4-A déjà figés.

Limite du harnais publié : `tools/run-local-tests.js` charge les `.gs` et son empreinte couvre `.gs/.html/.json`, mais pas les six `.js` historiques de `src/core/logger`. La vérification complémentaire a utilisé une copie locale du harnais avec `ScriptApp.getScriptId()` retournant l'identifiant RECETTE et la sélection serveur `/\.(gs|js)$/`. Ce périmètre complémentaire doit être conservé dans l'inventaire D4-B ; le compteur de tests ne remplace pas un inventaire de fichiers.

### 22.2 Candidate et construction du package

La candidate reste exclusivement `688c81bb64e6aa09f9955743b783fca989369ae2`. Le package portail est construit dans un répertoire neuf depuis cette référence, jamais depuis une branche locale non contrôlée. Il conserve les chemins sous `src`, les `.gs`, les `.js`, les `.html` et `appsscript.json`. Les README, outils Node, rapports et fichiers du package backend ne sont pas poussés.

| Inventaire Git | Base applicative | Candidate D4-A |
|---|---:|---:|
| `.gs` | 233 | 236 |
| `.js` serveur historiques | 6 | 6 |
| `.html` | 31 | 34 |
| Manifeste | 1 | 1 |
| Total déployable avant adaptation du manifeste | **271** | **277** |

Le total 271 obtenu en excluant les six `.js` de la candidate est incomplet. Les nombres historiques 261/269/271 décrivent d'autres états ; ils ne fixent pas le nombre de fichiers actuellement présent sur Google.

Empreinte locale du package Git complet avant adaptation du manifeste : `91871173349d843986aab23fc74271606e6562f79de8bdb2e5c9fbd1206b8580`. Algorithme : chemins relatifs POSIX sous `src` triés, puis pour chaque fichier chemin UTF-8, octet NUL, contenu exact, octet NUL ; SHA-256 final. Cette empreinte inclut les six `.js` et n'est pas celle du rapport D4-A. Une nouvelle empreinte doit identifier le package opérationnel avec son manifeste sauvegardé.

Le manifeste générique du dépôt contient `timeZone`, `dependencies`, `exceptionLogging` et `runtimeVersion`, sans `webapp`. D4-B doit conserver **les octets exacts du manifeste initial RECETTE sauvegardé** dans le package temporaire et dans la restauration. Cet écart explicite à Git est documenté dans le rapport de construction ; aucune section, audience, identité, dépendance ou scope n'est ajouté. Un manifeste inattendu entraîne un arrêt avant push. Ne jamais utiliser le manifeste backend `USER_DEPLOYING/ANYONE_ANONYMOUS` pour le portail.

### 22.3 B0 — préparation locale et précontrôle sans écriture Google

Cible portail unique : `1quyIoxSMlxe6xpADPlxRxGikRF3OCTEid0-xhOHeSRZH0sU0AOeIRxs4`. Le backend `1_C157CtD95GegcpxSrKcL6bRDyea1cPfH9NfJywBnTmJQzKY30qfNNUu` reste hors cible de push.

L'outillage doit être préparé et revu avant exécution, compatible Windows PowerShell 5.1, avec répertoire de lancement explicite `C:\AKS-Platform` et répertoire de campagne neuf sous `D:\AKS\ADMIN-006-D4B\run-<horodatage>`. Il doit réutiliser l'authentification locale existante sans afficher les jetons. Le protocole présent n'est pas un script exécutable et aucun précontrôle Google n'a encore été lancé.

Après autorisation de B0 en lecture seule :

1. Contrôler l'état local sans écraser les travaux, les versions Git/Node/clasp et le Script ID complet de la configuration RECETTE ; créer des racines séparées pour sauvegarde, candidate et relectures. Capturer immédiatement les codes de sortie natifs.
2. Relire le HEAD Google complet dans la sauvegarde neuve ; inventorier noms, types et contenus, archiver et calculer le SHA-256. Relire indépendamment pour confirmer une sauvegarde stable. Ne pas déduire le contenu du HEAD d'une version déployée.
3. Relever les inventaires complets des versions et déploiements et les paramètres du manifeste. Les URLs opérationnelles restent dans les rapports locaux protégés. Prévoir une fenêtre sans modification concurrente du HEAD ni usage du lien de développement.
4. Vérifier côté opérateur que le portail est fermé (`AKS_PRIVATE_PORTAL_ENABLED` absente ou `false`), que l'URL temporaire est absente et que le backend reste fermé. Les propriétés ne sont pas déduites d'un `clasp pull` : utiliser une capacité d'inspection existante et revue ou le panneau éditeur, sans coller les valeurs secrètes. Aucun inspecteur temporaire ni écriture de propriété n'est implicitement inclus dans B0 ; si cette vérification est indisponible, le précontrôle reste incomplet.
5. Construire les 277 fichiers de la candidate et substituer seulement son manifeste par la copie exacte sauvegardée. Calculer inventaire, archive et empreinte du package opérationnel. Vérifier les noms Apps Script normalisés pour éviter toute collision `.gs/.js` ou omission par la configuration clasp.
6. Produire le diff complet entre sauvegarde et package : ajouts, retraits, modifications, manifeste identique. Expliquer chaque écart, y compris les changements intégrés depuis le HEAD historique restauré ; le diff de la PR #145 seul ne suffit pas.
7. Arrêter B0 et soumettre son résultat avec les empreintes et le diff réel. L'autorisation B1 doit viser cette candidate, ce package, cette sauvegarde et le retrait des seuls fichiers supplémentaires identifiés pendant la restauration.

B0 n'effectue ni `push`, ni test Apps Script, ni création de version, ni modification de déploiement, de secret, d'ACCESS ou de partage. Les valeurs et empreintes Google inconnues ne doivent pas être préremplies depuis D3-D4.

### 22.4 B1 — validation temporaire et restauration, sous autorisation distincte

1. Vérifier de nouveau les références Git, l'intégrité du package et de la sauvegarde, la cible complète, les coupe-circuits fermés et l'absence de changement Google depuis B0. Si un état diffère, revenir au précontrôle avant toute mutation.
2. Pousser temporairement le package portail autorisé. Relire dans un troisième répertoire et comparer l'ensemble des noms/types/contenus et le manifeste ; un code de sortie nul ne prouve pas leur identité. Ne pas commencer les tests en cas d'écart.
3. Exécuter uniquement `AKS_runValidationSuiteV11` dans l'éditeur du portail, sous le compte opérateur autorisé. Résultat attendu pour cette candidate : **761/761, zéro échec**. Conserver le résumé et les échecs éventuels sans données métier. Les huit tests DOM restent une preuve Node distincte ; aucun appel à `AKS_readPrivatePortalLogs`, endpoint réel, activation ou parcours navigateur n'est inclus.
4. Restaurer le HEAD initial et son manifeste même après échec du push, de la relecture ou de la suite. Relire ensuite intégralement dans un répertoire neuf. En cas de fichiers résiduels, retirer uniquement ceux de la liste candidate moins sauvegarde, après vérification de leur contenu et de l'absence de modification concurrente. Aucun effacement global.
5. Si Apps Script conserve un champ de manifeste malgré le push, rétablir la copie initiale exacte dans l'éditeur, sous le périmètre de restauration autorisé, puis relire. Tant qu'un écart subsiste, déclarer la restauration incomplète et conserver les sauvegardes et moyens de récupération.
6. Comparer les inventaires de versions/déploiements et confirmer les coupe-circuits et propriétés non secrètes inchangés. Aucun secret n'étant modifié par B1, aucune réinstallation ni rotation n'est incluse. Consigner les contrôles réellement possibles ; ne pas déclarer une égalité de propriétés non observées.

Le script devra prévoir une restauration dans `finally` et une procédure de reprise indépendante utilisable après fermeture du terminal ou interruption. Un `finally` ne garantit pas l'exécution après l'arrêt du poste. Le temps d'exécution, le consentement OAuth éventuellement requis par les scopes inférés et les services réels restent à observer ; une demande inattendue entraîne un arrêt et une décision avant accord. Les essais d'intégration métier ou les fonctions de preuve D3 ne doivent pas être exécutés par erreur.

### 22.5 Rapport et critères de sortie

D4-B n'est conforme que si la candidate relue est exacte, les 761 tests Apps Script réussissent, la restauration relue est exacte, les inventaires de versions/déploiements sont inchangés et aucun périmètre exclu n'a été engagé. Un succès des tests suivi d'une restauration incomplète est un échec de campagne.

Le rapport doit identifier le run, l'opérateur, les références Git, la cible, les versions d'outils, les archives et SHA-256, le manifeste conservé, les inventaires et différences avant/après, le résultat de suite, les interventions manuelles, l'état de restauration et toute vérification non disponible. Aucun secret, signature, corps signé, URL backend ou contenu LOG ne doit rejoindre Git ou le chat.

Après compte rendu et revue de D4-B seulement : décision distincte de fusion applicative/documentaire puis cadrage D4-C. La preuve navigateur USER_ACCESSING, LOG_READ seul et retrait d'habilitation restent hors D4-B. Production et INSCRIPTIONS-011 restent non engagés.

## 23. Historique de préparation B0-r2 — avant la collecte Google

Le Product Owner a demandé « suivant » pendant la finalisation du protocole, puis a transmis les versions de son poste : PowerShell `5.1.26100.9168`, Node `v24.18.0`, clasp `3.3.0` et Git `2.45.1.windows.1`. L'outillage B0 est préparé dans la même PR documentaire #225 ; la candidate applicative #145 reste inchangée. Les outils d'exploitation ne font pas partie des 277 fichiers du portail.

### 23.1 Fichiers, prérequis et validation

Les trois fichiers suivants doivent rester dans un même dossier :

| Fichier | Responsabilité | SHA-256 |
|---|---|---|
| [Prepare-D4B.ps1](../../tools/admin006/Prepare-D4B.ps1) | Lanceur ASCII compatible par construction PowerShell 5.1 | `f43bb4eb73db80534bb91c43bf8db517a7a8c499acd76d37499ca8bbbe693bf5` |
| [prepare-d4b.cjs](../../tools/admin006/prepare-d4b.cjs) | Moteur Node de préparation et de lecture seule | `e19b4701d423bc676948e158a78f7bb4bf85490690538f6fbeafad684db9b52b` |
| [prepare-d4b.test.cjs](../../tools/admin006/prepare-d4b.test.cjs) | 14 tests synthétiques du moteur | `84e49eabc90f31e0615868bcf9c6eddb76165555acfa376dc288d3e15452a818` |

Prérequis : Windows PowerShell 5.1, Git, Node **20 ou supérieur**, et **clasp 3.3.0 ou 3.4.1 déjà installé** avec son authentification habituelle. Cette révision refuse une autre version clasp, sans installation ni mise à jour automatique. La révision r2 prend en charge clasp 3.3.0 présent sur le poste, sans mise à jour des outils ; le rapport consigne la version effectivement détectée. La cible et le commit candidat complets sont codés en constantes ; la configuration locale doit correspondre au portail RECETTE avec `rootDir=src`.

Les interfaces de clasp 3.3.0 et 3.4.1 ont été revues dans les [commandes JSON](https://github.com/google/clasp/blob/v3.3.0/src/commands/list-versions.ts) et la [pagination](https://github.com/google/clasp/blob/v3.3.0/src/core/utils.ts) : dix pages de cent résultats au maximum ; une liste atteignant mille éléments est donc refusée comme potentiellement tronquée. Les listes versions/déploiements sont collectées et comparées deux fois. Leurs descriptions et identifiants restent dans le dossier local.

Validation effectuée ici : **14/14 tests Node**, incluant collision de noms `.gs/.js`, manifeste backend refusé, pagination possiblement tronquée, cible incorrecte, absence d'appel Google en mode local, allowlist de lecture, échec natif, dérive du HEAD et conservation de la sauvegarde. L'extraction depuis les objets Git réels de la candidate a confirmé **277 fichiers** et l'empreinte complète du §22.2. Les collectes simulées vérifient les deux versions clasp acceptées et la version réellement inscrite dans le rapport. Le lanceur est ASCII ; la syntaxe Node et le diff ont été vérifiés.

Lors de la préparation, aucun parseur PowerShell ni poste Windows n'était disponible côté assistant. Le contrôle local a ensuite été exécuté sur le poste du Product Owner, selon le résultat transmis au §23.4. Cette preuve rapportée ne constitue ni un test Apps Script ni une lecture des propriétés Google.

### 23.2 Utilisation séquencée

Dossier proposé pour les trois outils : `D:\TMP\ADMIN006-D4B-r2`. Répertoire d'exécution : `C:\AKS-Platform`. Les fichiers doivent provenir ensemble du commit documentaire revu, avec les empreintes ci-dessus ; ne pas récupérer une tête mouvante pour une campagne figée.

Premier contrôle, entièrement local :

```powershell
Set-Location 'C:\AKS-Platform'
$D4BScript = 'D:\TMP\ADMIN006-D4B-r2\Prepare-D4B.ps1'
$D4BTokens = $null
$D4BParseErrors = $null
[void][System.Management.Automation.Language.Parser]::ParseFile(
    $D4BScript, [ref]$D4BTokens, [ref]$D4BParseErrors
)
if (@($D4BParseErrors).Count -ne 0) {
    $D4BParseErrors | ForEach-Object { Write-Host $_.Message }
    throw 'PowerShell parse failed. Stop.'
}
& $D4BScript -Mode LocalCheck
```

Le mode par défaut est `LocalCheck`. Il vérifie les prérequis et la configuration, lance les tests Node, extrait la candidate depuis les objets Git sans toucher à la branche de travail, puis archive les sources et l'inventaire dans un nouveau dossier sous `D:\AKS\ADMIN-006-D4B`. Il n'appelle pas clasp et ne contacte pas Google. Le commit candidat doit déjà être disponible localement ; s'il manque, le message `CANDIDATE_NOT_AVAILABLE_LOCALLY` invite à récupérer la branche Git avant de reprendre. Aucun `fetch`, installation, login ou consentement n'est déclenché automatiquement.

Après retour du contrôle local **et autorisation distincte des lectures Google B0**, relancer le même outil avec `-Mode GoogleReadOnly`. Ce mode autorise seulement `pull`, `list-versions` et `list-deployments`, sur la cible RECETTE complète et dans des répertoires neufs. Les appels natifs utilisent des tableaux d'arguments sans shell. Aucun mode push, run, version, déploiement ou propriété n'est implémenté.

L'installation npm globale standard est détectée à côté de la commande clasp ; une installation différente peut être désignée par `-ClaspPackage` avec le répertoire du paquet `@google/clasp`. Les identifiants d'authentification ne sont ni copiés dans le dossier de campagne, ni imprimés. Une authentification locale indisponible ou un accès refusé fait arrêter la collecte.

### 23.3 Rapports et limites de décision

Le dossier de campagne conserve les sources Git, deux lectures indépendantes, les listes de versions/déploiements, les inventaires, le diff complet et la liste des fichiers supplémentaires à retirer lors d'une future restauration. Les archives `*-bundle.json` sont des snapshots avec chemin, type, empreinte et contenu encodé en base64, permettant une reconstruction sans perte ; leurs SHA-256 figurent dans `report.json`. Ce format remplace une archive ZIP pour cette révision. Il ne contient aucune copie des fichiers d'authentification clasp.

Le package opérationnel conserve exactement le manifeste de la première lecture validée. Une section inattendue, des dépendances nouvelles, une audience ou identité différente provoquent un arrêt pour revue. Les `.gs` et `.js` sont comparés par leur nom et type Apps Script normalisés, tout en gardant leurs contenus exacts. Le répertoire candidat ne reçoit pas de configuration clasp de push.

Un résultat `LOCAL_CHECK_ONLY` ne prouve aucune lecture Google. Un résultat `READ_ONLY_COLLECTED_REVIEW_REQUIRED` signifie seulement que les fichiers et inventaires ont été collectés sans dérive observée ; il ne vaut pas conformité B0. `propertiesVerified` reste toujours `false` : le contrôle opérateur des coupe-circuits, de l'absence d'URL et des propriétés non secrètes, puis l'explication du diff, restent requis. Ne transmettre que le résumé expurgé et les empreintes utiles ; les snapshots et listes opérationnelles restent locaux.

En cas d'échec, le statut est `STOPPED`, aucun package n'est déclaré validé et les lectures déjà archivées sont conservées. Il n'y a aucune restauration Google à effectuer après B0, puisque le script ne réalise aucune écriture Google. B1 nécessite encore son propre outillage de validation/restauration revu et une autorisation sur les résultats concrets de B0. Aucune fusion n'est effectuée à ce stade.

### 23.4 Résultat du contrôle local Windows

Le Product Owner a transmis la sortie du bloc de contrôle local le 1er septembre 2026 :

| Contrôle | Résultat rapporté |
|---|---|
| Outils figés | B0-r2 depuis le commit documentaire `b2c1d26a4130aae6b31eeafd59114e4f3fa56c63` |
| Environnement | PowerShell `5.1.26100.9168`, Node `v24.18.0`, clasp `3.3.0`, Git `2.45.1.windows.1` |
| Tests Node sur Windows | **14/14**, zéro échec, zéro test annulé ou ignoré |
| Durée des tests | 1 017,8715 ms |
| Statut final | `LOCAL_CHECK_ONLY` |
| Dossier local | `D:\AKS\ADMIN-006-D4B\run-2026-09-01T13-37-35-830Z-57aefd` |
| Écriture Google | `false` |
| Propriétés | Contrôle opérateur requis |
| B1 | Non autorisé |

Le bloc fourni vérifie les trois empreintes et appelle le parseur PowerShell avant le lanceur. Son arrivée au statut final confirme le passage de ces contrôles selon l'exécution rapportée. L'assistant n'a pas lu directement le disque Windows ni le fichier `report.json` ; aucune empreinte supplémentaire de ce run n'est inventée à partir du résumé.

Le contrôle local Windows est conforme à son périmètre. Il ne valide pas B0 dans son ensemble : aucune lecture du HEAD Google, aucun inventaire réel de versions/déploiements ni aucun contrôle de propriétés n'a été effectué par ce mode. La prochaine décision est l'autorisation de B0 `GoogleReadOnly`, avec le même outillage figé et la même candidate applicative, pour deux lectures indépendantes du portail RECETTE, sauvegardes locales et diff. Aucune écriture Google, activation, exécution de test Apps Script, attribution ACCESS ou fusion n'entre dans cette étape.

## 24. B0 conforme — collecte opérateur et revue du diff

Après autorisation distincte, l'opérateur a exécuté `GoogleReadOnly` avec B0-r2 et clasp `3.3.0`. Run : `D:\AKS\ADMIN-006-D4B\run-2026-09-01T17-24-50-315Z-98f074`. Résultat transmis : **14/14 tests**, statut `READ_ONLY_COLLECTED_REVIEW_REQUIRED`, aucune écriture Google. Ce statut machine reste inchangé : la conformité résulte de la revue humaine complémentaire, sans réécriture de la preuve B0.

### 24.1 Références et empreintes

| Élément | Valeur vérifiée ou rapportée |
|---|---|
| Candidate applicative | `688c81bb64e6aa09f9955743b783fca989369ae2` |
| Source Git complète | 277 fichiers ; SHA-256 `91871173349d843986aab23fc74271606e6562f79de8bdb2e5c9fbd1206b8580` |
| Archive source Git | SHA-256 `372130e9f2d3cd3feb058fa22f87da88d7f4d25c7996ceb8fa93ba2f34fc27ac` |
| Sauvegarde HEAD portail | 261 fichiers ; archive SHA-256 `3daa853bd19b1de97ffbebe76ae6d273aa73cad0bb0574b20b8e60e0ce7c21de` |
| Archive du package adapté | SHA-256 `28bf17ac72ee8bf2b6c97d5749301861ef96de269345fb051b103793b5eebedf` |
| Empreinte du package adapté | SHA-256 `c332f690f9544353fb6686ed121cbfb3728432fb2b7e86efe3e23080e6769ff9` |
| Manifeste sauvegardé et conservé | SHA-256 `f9a8681074723b58dca5d4e55a3c35e76165aa1675909f498d5e2c0e907f9ddf` ; `Europe/Paris` ; section `webapp` absente |
| Stabilité | `independentReadExact: true` et `manifestUnchanged: true` dans le rapport |

Les archives et inventaires opérationnels restent sur le poste. L'assistant a relu le résumé et les 204 lignes nom/changement/SHA avant/après, pas les archives Google complètes. Les empreintes d'archives sont donc des valeurs rapportées ; B1 les recalculera localement avant toute opération. L'absence de section `webapp` dans le HEAD n'atteste pas l'identité d'exécution des déploiements existants.

### 24.2 Explication exhaustive des écarts transmis

Les 204 noms sont uniques ; les 204 empreintes après correspondent exactement aux octets Git de la candidate. Aucun retrait. Les 16 ajouts correspondent exactement au diff des sources depuis V1.4.0 ; les dix modifications de contenu ci-dessous ont des empreintes avant égales aux fichiers V1.4.0 avec fins de ligne CRLF, référence `fa8876fcc57dcc46b943c8a3ce451e006bfa5bb5`.

| Nombre | Nature | Explication |
|---|---|---|
| 178 modifications | Fins de ligne uniquement | Le SHA avant égale celui du fichier candidat converti en CRLF ; aucun écart de contenu inexpliqué |
| 6 modifications | V1.4.1 déjà intégrée dans Git | `Core`, `VersionApi`, `VersionTest`, `Access002AdminUiTest`, `AccessAccounts`, `AccessAccountsClient` : métadonnées de version et attribution Administration ; référence `7e5125e759703fa7628cceae1b7545a3dfc597e6` |
| 4 modifications | Intégration ADMIN-006 | `AdminDashboardController`, `AdminLogController`, `TestSuiteV11`, `Dashboard` : raccordement privé et tests cumulatifs |
| 16 ajouts | ADMIN-006 | Un contrôleur, sept fichiers `core/private`, cinq fichiers de tests et trois vues/client UI ; liste nominative locale `restoration-residuals.json` |

Les six fichiers V1.4.1 ne constituent pas de nouveaux changements applicatifs dans cette livraison : ils expliquent l'écart avec le HEAD RECETTE historique. De même, la présence du code backend dans le package complet du portail n'autorise aucune écriture dans le projet backend séparé. L'architecture et la preuve D3-D4 ne sont pas remises en cause.

### 24.3 Contrôle opérateur et décision

L'opérateur confirme :

- portail RECETTE `…eIRxs4` : `AKS_PRIVATE_PORTAL_ENABLED` absente et `AKS_PRIVATE_BACKEND_URL` absente ;
- backend privé RECETTE `…fNNUu` : `AKS_PRIVATE_ENABLED` à `false`.

Ces constats manuels ne sont pas déduits de `clasp pull` ; le champ B0 `propertiesVerified: false` reste honnêtement inchangé. Sur la base du rapport, de la revue du diff et de cette confirmation, **B0 est déclaré conforme**, décision validée par le Product Owner. Aucun push, test Apps Script, changement de version/déploiement, activation, secret, ACCESS ou fusion n'est intervenu dans B0. Cette conformité n'est pas une autorisation B1.

## 25. Historique de préparation et d'autorisation B1-r1

Le Product Owner autorise la consignation de B0 et la préparation/revue de B1, pas son exécution. Les trois fichiers ajoutés dans `tools/admin006` sont `Validate-D4B.ps1`, `validate-d4b.cjs` et `validate-d4b.test.cjs`. Le moteur réutilise `prepare-d4b.cjs` B0-r2, dont il contrôle l'empreinte avant chargement. Aucun outil n'est ajouté au package applicatif.

| Fichier requis | SHA-256 |
|---|---|
| `Validate-D4B.ps1` | `25b8096e704ac79d4db1db93cc144989fd10c3effe9ef38d4fdf3260ba73a749` |
| `validate-d4b.cjs` | `3461f26c5b3b920ff5cb123a727a55562a1ba98eefad72c3fc6d3c828418df51` |
| `validate-d4b.test.cjs` | `9532e70bec7c8cb7a1cdfc394d2f4ec7a17b3387d370c467c2177d44abbc977b` |
| `prepare-d4b.cjs` (B0-r2 inchangé) | `e19b4701d423bc676948e158a78f7bb4bf85490690538f6fbeafad684db9b52b` |

### 25.1 Limites et garde-fous

- Mode par défaut `LocalCheck` : lecture locale des archives B0, reconstitution et contrôle du package contre Git ; aucun appel clasp, aucune session B1 créée et aucune écriture Google.
- PowerShell 5.1, Node 20+ et **clasp 3.3.0** existant. B1 ne reprend pas implicitement la compatibilité 3.4.1 de B0 : seule l'interface de push 3.3.0 a été relue. Aucune installation ou mise à jour automatique.
- L'exécution exige une autorisation explicite liée au package `c332f690…`, puis une confirmation interactive des références, coupe-circuits et de la fenêtre sans modification concurrente ni usage du lien de développement.
- Cible complète figée sur le portail RECETTE. Archives, octets UTF-8, SHA, noms/types, manifeste, diff et liste des résidus vérifiés ; refus des collisions, chemins dangereux et archives altérées. Les 277 fichiers et les six `.js` sont conservés.
- Seules opérations clasp implémentées : `pull`, `list-versions`, `list-deployments`, `push --force`. Aucun `run`, login, version, déploiement, propriété ou partage. `--force` sert uniquement à envoyer le manifeste sauvegardé exact sans dialogue clasp ; il n'autorise pas de modification de manifeste.
- Relecture fraîche du HEAD et des inventaires avant push, comparaison exacte à B0 ; relecture complète après push avant toute invitation à tester. Chaque lecture encadre le pull par deux inventaires stables de versions/déploiements.
- Seul test autorisable : `AKS_runValidationSuiteV11`, manuellement dans l'éditeur portail, attendu **761/761 et zéro échec**. Aucune exécution réelle n'est revendiquée par les tests Node. Un consentement inattendu impose l'arrêt avant accord ; ne pas exécuter une autre fonction.
- Restauration en `finally`, y compris après erreur du push ou de la relecture, échec de test ou fin de saisie. Avant restauration, les fichiers distants doivent être des versions connues de la sauvegarde/candidate et les inventaires inchangés ; toute modification inconnue bloque l'écrasement et impose une revue humaine.
- Relecture exacte obligatoire après restauration. Aucun succès de test ne masque une restauration incomplète. Confirmation manuelle des propriétés en sortie ; le résultat conserve séparément `restoredExact`, `testPassed` et `propertiesOperatorConfirmed`.

L'interface de push relue est celle de [clasp v3.3.0, commande push](https://github.com/google/clasp/blob/v3.3.0/src/commands/push.ts) et de son [gestionnaire de fichiers](https://github.com/google/clasp/blob/v3.3.0/src/core/files.ts). La détection des changements n'énumère pas les seules suppressions distantes : un push retournant zéro fichier, notamment en présence de seuls résidus, n'est jamais assimilé à une restauration. Le retrait manuel éventuel reste limité aux 16 noms et empreintes contrôlés de `restoration-residuals.json`, sous l'autorisation de restauration prévue, puis suivi d'une relecture.

### 25.2 Interruption et reprise

Avant l'appel de push, un événement `CANDIDATE_PUSH_INTENT` est enregistré et synchronisé sur disque dans une session neuve sous `D:\AKS\ADMIN-006-D4B-B1\b1-…`. La session lie la cible, la candidate, les deux archives et l'empreinte des inventaires. Les événements, résultats et relectures sont ajoutés sans écraser les précédents.

Le mode séparé `Restore` recharge cette session et les archives B0, sans Git ni exécution d'un nouveau test. Il exige l'autorisation B1/restauration, la session exacte et la confirmation qu'aucun test Apps Script ne tourne encore. Il vérifie de nouveau l'état distant avant d'écrire, ne pousse pas si la sauvegarde est déjà exacte, et ne déclare jamais à lui seul les tests réussis.

Un `finally` ne couvre pas une coupure du poste ni toute terminaison forcée d'un processus natif. Ne pas fermer le terminal pendant un test : attendre sa fin ou son annulation effective. Après interruption, suspendre tout usage du HEAD et reprendre avec `Restore`, sans relancer `Execute`. Un changement concurrent, une identité/cible inattendue ou un problème de permission impose un arrêt et une décision, pas un contournement. Il n'existe pas de verrou atomique entre lecture et écriture de l'API : la fenêtre opérateur sans modification concurrente est indispensable.

Si le manifeste ou un fichier devient inconnu, ou si les 16 résidus ne disparaissent pas, conserver les preuves et demander la revue de l'écart avant intervention ciblée. Aucun effacement global, réessai automatique, changement de sauvegarde ou suppression des archives n'est prévu.

### 25.3 Validation locale et prochaine étape

**39/39 tests B1 synthétiques et 14/14 tests B0 passent**, soit 53/53. Ils couvrent notamment la liaison aux preuves B0, la préparation sans Google, les refus avant écriture, l'échec avant/après push simulé, l'EOF, les écarts de relecture, les modifications concurrentes, les résidus, la reprise persistante et la confirmation séparée des propriétés. Syntaxe Node et revue statique du lanceur PowerShell ASCII contrôlées ; PowerShell n'est pas installé dans l'environnement de revue. Le contrôle réel PowerShell 5.1 et le `LocalCheck` sur les archives opérateur restent à exécuter.

Après récupération des quatre fichiers nécessaires depuis un commit documentaire figé et vérification de leurs empreintes, lancer depuis `C:\AKS-Platform` uniquement :

```powershell
& 'D:\TMP\ADMIN006-D4B-B1-r1\Validate-D4B.ps1' -Mode LocalCheck
```

Résultat attendu : **39/39 tests** puis `B1_LOCAL_CHECK_ONLY`. Ce résultat n'atteste ni l'état actuel de Google, ni une exécution de test Apps Script. Les commandes d'exécution et de reprise ne seront fournies qu'avec l'autorisation B1 distincte, après revue du contrôle Windows et reconfirmation des références et propriétés. Les PR #145 et #225 restent ouvertes, sans fusion ; aucune activation ni modification de production.

### 25.4 Contrôle Windows conforme et autorisation B1

L'opérateur a exécuté le bloc de récupération figé sur le commit documentaire `b7bcf1cc948e59fdbdc269c340f450259eef7a9f`, puis transmis **39/39 tests réussis**, zéro échec, durée `585.2821 ms`, statut `B1_LOCAL_CHECK_ONLY` et `googleWriteAttempted: false`. Le bloc prévoyait la vérification des quatre empreintes et l'analyse syntaxique PowerShell avant lancement. Le retour confirme le contrôle local ; il ne constitue pas une observation de Google ni une exécution de la suite Apps Script.

Après ce résultat, le Product Owner répond « validé » à l'autorisation explicite de B1 : push temporaire de la candidate `688c81bb64e6aa09f9955743b783fca989369ae2` sur le seul portail RECETTE, relecture exacte, exécution manuelle de `AKS_runValidationSuiteV11` (761/761 attendus), restauration de la sauvegarde B0 et relecture exacte. L'autorisation couvre la restauration après échec/interruption et, si nécessaire, le retrait des seuls 16 fichiers supplémentaires identifiés et vérifiés. Les empreintes de package/sauvegarde du §24.1 et les outils du §25 restent inchangés.

Sont exclus : activation, modification de propriété ou de secret, opération sur le projet backend ou la production, création de version, modification de déploiement, attribution ACCESS et fusion. Les coupe-circuits et l'absence d'URL doivent être reconfirmés avant l'exécution, puis après restauration. Toute dérive depuis B0 ou demande de consentement inattendue impose un arrêt avant nouvelle action hors périmètre.

Le précontrôle Git de cette autorisation retrouve les PR #145 et #225 ouvertes et non fusionnées, respectivement aux têtes `688c81bb64e6aa09f9955743b783fca989369ae2` et `b7bcf1cc948e59fdbdc269c340f450259eef7a9f`, sur les bases inchangées. La mise à jour présente ne touche que la documentation et la description de #225. Aucun résultat Google B1 n'est encore reçu ; ne pas déclarer D4-B conforme avant les preuves de test, de restauration et de propriétés.

## 26. B1 en échec, restauration exacte rapportée et correction des tests

### 26.1 Résultat opérateur de la campagne initiale

Session : `D:\AKS\ADMIN-006-D4B-B1\b1-2YMQP2`. Candidate testée : `688c81bb64e6aa09f9955743b783fca989369ae2`, package et sauvegarde du §24.1.

| Champ du rapport transmis | Valeur |
|---|---|
| `status` | `TEST_NOT_PASSED` |
| `testPassed` | `false` |
| `restoredExact` | `true` |
| `googleWriteAttempted` | `true` |
| `propertiesOperatorConfirmed` | `true` |

La suite cumulative rapporte deux échecs :

1. `recipeDashboardRendersBeforePrivateClient` : `Cannot read properties of null (reading 'status')` ;
2. `recipeWithoutLogReadDoesNotLoadWidget` : `Expected condition to be true.`

Le code de sortie 1 et l'exception finale PowerShell signalent ici l'échec des tests, pas l'échec de la restauration. Le moteur rapporte une relecture de restauration exacte et vérifie alors aussi les inventaires ; les propriétés sont confirmées par l'opérateur. L'assistant n'a pas relu directement les archives de cette session. **B1 et D4-B restent non conformes ; la restauration est rapportée réussie.** Il ne faut relancer ni `Execute` ni une restauration déjà achevée.

### 26.2 Diagnostic reproduit sans Google

Sur les 277 sources exactes de la candidate initiale, le harnais hors ligne reproduit 761/761 avec une URL Web App HTTPS valide, contre 759/761 et les deux mêmes erreurs avec une URL vide ou `null`. La valeur réelle renvoyée par Google pendant l'exécution n'a pas été interrogée : la reproduction établit la dépendance non isolée, sans prétendre avoir observé cette valeur distante.

Les tests remplaçaient ACCESS et le client privé, mais pas le fournisseur d'URL utilisé par la projection du portail. Sans URL exploitable, la projection retire les destinations : le widget reste `null` et l'action Paramétrage attendue disparaît. Le harnais initial fournissait toujours une URL valide, ce qui masquait cette dépendance. Aucun déploiement ni ajout d'URL dans les propriétés n'est requis pour corriger ces tests.

### 26.3 Correction ciblée autorisée et livrée

Le Product Owner autorise la correction des tests et du harnais dans #145 et la consignation dans #225, sans opération Google ni fusion. Nouvelle candidate : **`c39cded9f8d17493780a03cc66e408158ebb5d2d`**.

- Seul fichier modifié sous `src` depuis la candidate initiale : `tests/admin/Admin006PrivatePortalRuntimeTest.gs`. La fixture conserve le vrai moteur de projection et injecte seulement une URL de navigation synthétique à travers son fournisseur existant. Aucun service Google natif n'est remplacé ; la projection d'origine est rétablie dans `finally`.
- Assertions renforcées : destination Journaux correcte avec LOG_READ, absence de cette destination sans LOG_READ, restauration de toutes les dépendances de test après succès et après exception synthétique.
- `tools/run-local-tests.js` ajoute `--url-matrix`, `--recipe` et les cas `--web-app-url=valid`, `empty`, `null`. Chaque scénario tourne dans un processus indépendant. Le harnais charge désormais les six `.js` en plus des `.gs` et son empreinte couvre les 277 fichiers déployables avec chemins POSIX triés.
- Preuve ajoutée : [admin006-d4b-url-matrix.json](https://github.com/karateseremange/AKS-Platform/blob/c39cded9f8d17493780a03cc66e408158ebb5d2d/docs/validation/admin006-d4b-url-matrix.json). La preuve D4-A initiale est conservée, pas réécrite.

| URL simulée | Projet tiers | RECETTE |
|---|---|---|
| HTTPS valide | 761/761 | 761/761 |
| Chaîne vide | 761/761 | 761/761 |
| `null` | 761/761 | 761/761 |

Les six scénarios réexécutent les mêmes 761 tests, dont 32 D4-A ; il ne s'agit pas de 4 566 tests distincts. Les huit tests DOM simulés passent également. Nouvelle empreinte source complète : `12aaaa3bda045a7a0bc03cdcf4919d599c2e0cb8eba88489ba273ea6a7d13053` ; 277 fichiers, dont 242 sources serveur. Cette empreinte inclut les `.js`, contrairement à celle du rapport D4-A initial ; elle ne désigne pas encore un package adapté au manifeste sauvegardé.

Limite exploratoire : une exception forcée de `getUrl()` (distincte d'un retour vide ou `null`) fait encore échouer le test de route page `recipePageAndGettersNeverUseDirectRepository` en simulation, à 760/761. Ce cas ne correspond pas aux deux erreurs opérateur et n'est pas corrigé ici ; la matrice publiée couvre les trois valeurs annoncées, pas la robustesse générique aux exceptions des services natifs. Aucun résultat Apps Script n'est revendiqué pour la nouvelle candidate.

### 26.4 Suite et frontières d'autorisation

Les contrôleurs, le code fonctionnel, les vues, le backend, le protocole, l'anti-rejeu et les manifestes sont inchangés par cette correction. Les outils B0-r2/B1-r1, leurs archives et l'ancienne session sont conservés sans modification pour la traçabilité ; ils restent liés à l'ancienne candidate et ne doivent pas servir à relancer son `Execute`.

Avant un nouvel essai : préparer et revoir un outillage lié à la nouvelle candidate, revalider la sauvegarde et l'état distant sous autorisation de lecture adaptée, produire le nouveau package et ses empreintes, contrôler localement puis recueillir une **nouvelle autorisation B1**. L'autorisation initiale ne couvre pas implicitement la nouvelle candidate. Aucun nouveau push, test Apps Script, propriété, activation, déploiement, production ou fusion n'a été effectué pendant cette correction.

## 27. C2-local-r1 — reconstruction locale de la nouvelle candidate

Le Product Owner demande la suite après la correction des tests : préparation du précontrôle et du nouveau package, sans opération Google. Trois nouveaux fichiers sont livrés dans `tools/admin006` : `Rebuild-D4B.ps1`, `rebuild-d4b.cjs`, `rebuild-d4b.test.cjs`. Les outils B0/B1 initiaux restent inchangés. C2-local-r1 réutilise seulement leurs lecteurs et validateurs de preuves, avec contrôle de leurs empreintes avant chargement ; il n'appelle aucun de leurs modes Google.

### 27.1 Sources de preuve et périmètre

- Extraction des objets Git au commit exact `c39cded9f8d17493780a03cc66e408158ebb5d2d`, sans lire ni modifier le code du répertoire de travail : 277 fichiers, SHA source `12aaaa3bda045a7a0bc03cdcf4919d599c2e0cb8eba88489ba273ea6a7d13053`.
- Relecture locale des archives B0 du §24, avec validation de leurs SHA, du manifeste, des inventaires et du diff historique.
- Contrôle de la session `b1-2YMQP2` du §26 : liaison aux preuves B0, résultat unique `TEST_NOT_PASSED` avec restauration/propriétés confirmées, puis comparaison des fichiers réellement présents dans sa relecture locale `after-restore-…` avec la sauvegarde. Plusieurs résultats ou relectures imposent une revue, sans choisir arbitrairement le plus favorable.
- Reconstruction dans un répertoire neuf sous `D:\AKS\ADMIN-006-D4B-C2\local-c2-…`. Le manifeste est repris octet pour octet de la sauvegarde B0 ; aucune configuration clasp n'est installée dans le dossier candidat.
- Le seul écart admis avec l'ancien package est `tests/admin/Admin006PrivatePortalRuntimeTest.gs`. Le diff avec la sauvegarde conserve 16 ajouts, 188 modifications, zéro retrait et les mêmes 16 noms de résidus ; l'empreinte du fichier de tests ajouté est naturellement mise à jour.
- Aucun appel clasp, aucune lecture/écriture Google, aucune exécution de test Apps Script et aucun jeton d'autorisation B1 ne sont implémentés dans ce nouveau flux.

L'outil ne transforme pas les anciennes preuves en constat actuel : `historicalRestorationSnapshotVerifiedLocally` décrit une comparaison locale, et `historicalPropertiesOperatorConfirmed` reprend le rapport initial. `remoteStateRevalidated`, `currentPropertiesVerified` et `b1Authorized` restent tous à `false`.

### 27.2 Fichiers figés et contrôles locaux

| Fichier requis | SHA-256 |
|---|---|
| `Rebuild-D4B.ps1` | `89dddab2ddabd7abfbd441853b5d9b36a217034b01a885f7c677efa423789a27` |
| `rebuild-d4b.cjs` | `1333d58250a76f7aa52cf50e57857ad7daf4bfb743cb5d91291f433d61c35b4f` |
| `rebuild-d4b.test.cjs` | `58501e21ebff70bc3eb255fe8484c8b73391895724956ecb409255dfc70a9edb` |
| `prepare-d4b.cjs` (inchangé) | `e19b4701d423bc676948e158a78f7bb4bf85490690538f6fbeafad684db9b52b` |
| `validate-d4b.cjs` (inchangé) | `3461f26c5b3b920ff5cb123a727a55562a1ba98eefad72c3fc6d3c828418df51` |

**17/17 tests C2 synthétiques passent**, ainsi que les 53 tests B0/B1, soit 70/70. Les tests couvrent la session, les archives/readbacks locaux, les preuves ambiguës ou altérées, la conservation du manifeste, les changements fonctionnels refusés, les répertoires protégés, les constructions répétées sans écrasement et les états de validation explicitement distincts. L'extraction Git réelle retrouve les 277 fichiers et le SHA attendu. Syntaxe Node vérifiée ; lanceur ASCII relu pour PowerShell 5.1. La reconstruction avec les archives réelles et le contrôle PowerShell restent à exécuter sur le poste opérateur.

### 27.3 Exécution locale et décision suivante

Depuis `C:\AKS-Platform`, récupérer le commit applicatif exact par `git fetch` sans changer de branche ni de fichiers, puis les cinq fichiers d'outillage depuis un commit documentaire figé dans un nouveau dossier `D:\TMP\ADMIN006-D4B-C2-r1`. Vérifier les empreintes et analyser le lanceur avec le parseur PowerShell 5.1 avant :

```powershell
& 'D:\TMP\ADMIN006-D4B-C2-r1\Rebuild-D4B.ps1'
```

Le programme produit une nouvelle archive candidate, son inventaire, le diff sauvegarde/candidate, le delta avec l'ancien package, la liste des résidus et `report.json`. Résultat attendu : **17/17 tests**, puis `LOCAL_REBUILT_REMOTE_REVIEW_REQUIRED`, avec `googleReadExecuted: false`, `googleWriteAttempted: false` et `b1Authorized: false`. Transmettre seulement ce résumé et les empreintes ; conserver localement les archives, sources et inventaires opérationnels.

Les nouvelles empreintes du package adapté et de son archive ne sont pas préremplies : elles seront recalculées avec le manifeste sauvegardé réel et relues dans le résultat opérateur. Après revue de ce résultat, préparer la revalidation distante en lecture seule et l'outillage B1 lié à ces preuves, avec les autorisations distinctes nécessaires. Aucun ancien `Execute` ne doit être relancé ; les anciennes archives et la session restaurée ne sont ni écrasées ni supprimées.

### 27.4 Reconstruction Windows rapportée conforme

L'opérateur transmet **17/17 tests réussis**, zéro échec, durée `1147.678 ms`, et le rapport généré le `2026-09-01T21:29:06.476Z` dans `D:\AKS\ADMIN-006-D4B-C2\local-c2-G8mK7N`. Le statut est `LOCAL_REBUILT_REMOTE_REVIEW_REQUIRED` ; la reconstruction est conforme au périmètre local selon ce retour, pas une validation de l'état actuel Google.

| Preuve transmise | Valeur |
|---|---|
| Candidate | `c39cded9f8d17493780a03cc66e408158ebb5d2d` |
| Source complète, 277 fichiers | `12aaaa3bda045a7a0bc03cdcf4919d599c2e0cb8eba88489ba273ea6a7d13053` |
| Package adapté, 277 fichiers | `d2766f9e55e5795ab43df099a1aae3414183aab2aa45f07c3e92654371717d99` |
| Archive du package | `571d7841fbf3ee6cb2f156fb3285adb205ecb1ada4f50a66ad635ef3a11aa07a` |
| Archive sauvegarde, 261 fichiers | `3daa853bd19b1de97ffbebe76ae6d273aa73cad0bb0574b20b8e60e0ce7c21de` |
| Manifeste inchangé | `f9a8681074723b58dca5d4e55a3c35e76165aa1675909f498d5e2c0e907f9ddf` |

Le delta avec le précédent package ne contient que le fichier de tests attendu, de `93c31390e276233db07d5fe941c2ecb8904512a42da026f54d2b06e86c016375` à `d1a8d12b6bec6069aba4c8ad76169ed442c29bb0962ca6aca24dbb3f3990e6a5`. Diff avec la sauvegarde : 16 ajouts, 188 modifications, aucun retrait. La restauration historique et les propriétés historiques sont confirmées dans le rapport local. `currentPropertiesVerified`, `remoteStateRevalidated`, `googleReadExecuted`, `googleWriteAttempted` et `b1Authorized` restent à `false`.

## 28. C2-readonly-r1 — préparation du précontrôle autorisé

Après revue du résultat C2, le Product Owner autorise le précontrôle **en lecture seule du portail RECETTE**, puis accepte sa préparation via GitHub avec exécution des tests sur son poste. Le terminal de l'assistant n'est pas disponible ; l'accès GitHub est fonctionnel. Cette autorisation ne renouvelle pas B1.

### 28.1 Périmètre et garde-fous

Trois fichiers supplémentaires dans `tools/admin006` : `Check-D4B-C2.ps1`, `check-d4b-c2.cjs`, `check-d4b-c2.test.cjs`. Aucun outil historique n'est modifié.

- Mode par défaut `LocalCheck` : archives B0/C2, empreintes, manifeste, delta et compte de fichiers vérifiés localement ; résolution de clasp 3.3.0 sans l'exécuter. Aucun appel Google ni création de campagne distante.
- Mode explicite `ReadOnly` : cible figée `1quyIoxSMlxe6xpADPlxRxGikRF3OCTEid0-xhOHeSRZH0sU0AOeIRxs4`, deux répertoires neufs et deux lectures indépendantes. Chaque lecture encadre `pull` par les inventaires versions/déploiements avant et après, soit dix appels clasp au total.
- Chaque HEAD doit correspondre exactement à la sauvegarde B0 (261 fichiers) et les inventaires doivent rester identiques aux preuves B0. Une divergence provoque `STOPPED`, conservation des preuves disponibles et revue humaine, sans correction ni restauration automatique.
- Réutilisation du seul lecteur `snapshot` historique après contrôle des SHA des dépendances. Un garde-fou natif indépendant n'accepte que les arguments exacts `--json pull`, `--json list-versions`, `--json list-deployments`. Même un appel accidentel au `push` du module historique est refusé avant lancement du processus.
- Liaison au package C2 du §27.4, et jeton de lecture seule distinct `C2-READONLY-` suivi de son SHA de package. Aucun mode `Execute`, `Restore`, test Apps Script ou jeton B1 n'est proposé.
- Dossiers de preuves préservés, nouvelle session à chaque lecture, sorties natives non publiées. Les archives complètes et inventaires restent sur le poste.
- Les propriétés ne sont pas vérifiées automatiquement : `propertiesOperatorConfirmed` et `b1Authorized` restent à `false`, y compris après succès des lectures.

Aucune activation, modification de propriété/secret, opération backend/production, création de version, modification de déploiement, attribution ACCESS ou fusion. Une erreur de permission ou identité inattendue impose l'arrêt et une décision ; pas de contournement.

### 28.2 Validation encore requise sur Windows

**20 tests synthétiques sont fournis, mais n'ont pas été exécutés par l'assistant.** Ils couvrent les modes, le jeton séparé, les commandes interdites, les preuves altérées, le delta, le manifeste, les deux lectures, les dérives, les journaux d'échec, la protection des répertoires et le transport historique avec processus simulé. Les sources CommonJS ont seulement été analysées syntaxiquement avec V8 ; ce n'est ni `node --check`, ni `node --test`. Le lanceur ASCII est relu pour PowerShell 5.1, dont le parseur reste à exécuter sur le poste.

| Fichier requis | SHA-256 |
|---|---|
| `Check-D4B-C2.ps1` | `c62796179cb836736ca84d41f6d99a11b677307e4a404ca0a065ce0b9ecee811` |
| `check-d4b-c2.cjs` | `fd28976e887cd4f3ad77707d61aa75d1287d4a5a701ee5e4ecbfd661d697d170` |
| `check-d4b-c2.test.cjs` | `2504550b84b8f67843dd30a3b8db6af52c8df888a4fcf568091dc2e78b74614d` |
| `prepare-d4b.cjs` | `e19b4701d423bc676948e158a78f7bb4bf85490690538f6fbeafad684db9b52b` |
| `validate-d4b.cjs` | `3461f26c5b3b920ff5cb123a727a55562a1ba98eefad72c3fc6d3c828418df51` |

### 28.3 Prochaine étape exclusivement locale

Récupérer les cinq fichiers du §28.2 depuis le commit documentaire figé dans un nouveau dossier `D:\TMP\ADMIN006-D4B-C2-READONLY-r1`, vérifier leurs empreintes et analyser le lanceur avec le parseur PowerShell. Depuis `C:\AKS-Platform`, exécuter uniquement :

```powershell
& 'D:\TMP\ADMIN006-D4B-C2-READONLY-r1\Check-D4B-C2.ps1' -Mode LocalCheck
```

Attendu : 20 tests réussis puis `C2_READONLY_LOCAL_CHECK_ONLY`, avec `googleReadAttempted: false`, `googleWriteAttempted: false`, `propertiesOperatorConfirmed: false`, `b1Authorized: false`. Arrêt au premier échec ; transmettre seulement le résumé et le JSON.

Après revue du résultat local, le mode `ReadOnly` pourra être exécuté au titre de l'autorisation déjà reçue. Son succès attendu `READ_ONLY_COLLECTED_PROPERTIES_REVIEW_REQUIRED` devra être suivi d'une confirmation manuelle actuelle des propriétés : les deux propriétés privées du portail absentes et `AKS_PRIVATE_ENABLED` du backend à `false`, sans aucune modification. Les constats historiques ne remplacent pas cette vérification. Le nouvel outillage et l'autorisation d'écriture B1 restent des étapes distinctes ultérieures.

## 29. Historique

| Version | Date | Évolution |
|---|---|---|
| 0.16.0 | 2026-09-01 | Reconstruction C2 rapportée conforme (17/17) et empreintes figées ; lecture seule autorisée, outillage préparé via GitHub et 20 tests Windows encore requis, aucun nouveau B1 |
| 0.15.0 | 2026-09-01 | C2-local-r1 préparé pour la nouvelle candidate : reconstruction sans Google depuis les preuves B0/B1, 17/17 tests et extraction Git exacte ; contrôle Windows et empreintes adaptées encore attendus |
| 0.14.0 | 2026-09-01 | B1 initial échoué sur deux tests et restauré exactement selon l'opérateur ; dépendance à l'URL isolée dans les tests, nouvelle candidate c39cded9, six scénarios locaux à 761/761 ; nouveau package et nouvelle autorisation B1 requis |
| 0.13.1 | 2026-09-01 | Contrôle Windows B1-r1 rapporté conforme (39/39, B1_LOCAL_CHECK_ONLY) ; autorisation distincte B1 et restauration reçue, outils figés inchangés, exécution Google et résultats attendus |
| 0.13.0 | 2026-09-01 | B0 conforme après collecte opérateur, explication des 204 écarts et confirmation des propriétés ; outillage B1-r1 préparé avec 39/39 tests synthétiques, contrôle Windows requis et exécution B1 non autorisée |
| 0.12.2 | 2026-09-01 | Contrôle Windows B0-r2 rapporté conforme : 14/14 et LOCAL_CHECK_ONLY, run 13:37:35 ; lecture Google et contrôle des propriétés non exécutés, B1 non autorisé |
| 0.12.1 | 2026-09-01 | B0-r2 adapté au poste : clasp 3.3.0 accepté après revue des interfaces, 14/14 tests Node, version effective dans le rapport ; aucune mise à jour des outils ni opération Google |
| 0.12.0 | 2026-09-01 | Outillage B0-r1 livré : lanceur PowerShell et moteur Node, 13/13 tests synthétiques, extraction Git complète vérifiée ; parseur PowerShell/Windows et lectures Google non exécutés |
| 0.11.0 | 2026-09-01 | Revue conjointe D4-A sans défaut bloquant identifié ; contrôles locaux RECETTE et six .js à 761/761 ; protocole D4-B en B0 lecture seule puis B1 réversible, 277 fichiers et manifeste sauvegardé, aucune exécution Google |
| 0.10.0 | 2026-09-01 | D4-A livré en PR applicative #145, runtime inactif, vues privées, 761/761 tests locaux et 8/8 client simulé ; D4-B et preuve navigateur requis, aucune opération Google |
| 0.9.1 | 2026-09-01 | Correction documentaire autorisée de la PR #224 : fenêtre des 500 dernières lignes, absence d'exhaustivité historique, information des résultats vides et tests à implémenter ; backend, architecture et validation D3-D4 inchangés |
| 0.9.0 | 2026-09-01 | Plan D4 proposé : précontrôle GitHub, routes privées, isolation, sécurité, tests et restauration ; aucune opération Google |
| 0.8.0 | 2026-08-30 | D3-A intégré et validé à 729/729, restauration exacte ; D3-B support de preuve et propriétés non secrètes requis avant push |
| 0.7.0 | 2026-08-30 | Précontrôle D3 : code non déployable en l’état, endpoint, adaptateurs, package backend et décisions d’exploitation requis ; aucune mutation externe |
| 0.6.0 | 2026-08-30 | D2-B terminé : secret courant et version concordants, clé précédente absente, transport désactivé |
| 0.5.0 | 2026-08-30 | D2-A terminé : propriétés inventoriées par noms et empreintes, restaurations exactes ; D2-B cadré sans installation |
| 0.4.0 | 2026-08-30 | Précontrôle D2 terminé : propriétés non observables par Drive ou clasp, inspecteur réversible requis avant toute installation |
| 0.3.0 | 2026-08-30 | D1 terminé : backend privé vide et support LOG dédié créés sans partage, secret, déploiement ni raccordement |
| 0.2.0 | 2026-08-30 | D0 terminé : portail confirmé, POC et Services refusés, backend et support LOG dédiés recommandés dans ADMIN-006-09 |
| 0.1.0 | 2026-08-30 | Cadrage du lot D : inventaire, identités, backend privé, secret HMAC, anti-rejeu, preuves, recette multi-compte et retour arrière, sans opération Google |
