# ADMIN-006-13 — Précontrôle D3 du backend privé RECETTE

| Propriété | Valeur |
|---|---|
| **Document ID** | ADMIN-006-13 |
| **Titre** | Précontrôle du code, de la configuration et du déploiement backend |
| **Version** | 0.5.0 |
| **Statut** | D3-D cadré dans ADMIN-006-14 — D3-D1 requis |
| **Nature** | Inventaire technique, analyse d’écarts et plan de mise en œuvre |
| **Propriétaire** | Product Owner |
| **Dernière mise à jour** | 2026-08-30 |
| **Dépendances** | ADMIN-006, ADMIN-006-08, ADMIN-006-10, ADMIN-006-12, SECURITY-001, LOG-001 |
| **Référence applicative** | `develop@bc8b3be19c3860079db0228769d8cb1d80f302dc` |
| **Référence Project Book** | `develop@1b91ada69acaa1c7ef581f5aa544a97e029589bf` |
| **Environnement** | RECETTE uniquement |

---

## 1. Objet

Ce document consigne le précontrôle D3 autorisé en lecture seule. Il détermine si le code actuellement intégré dans le dépôt applicatif peut être assemblé et déployé tel quel dans le projet `AKS Private Backend RECETTE`.

Aucun fichier applicatif, projet Apps Script, secret, support Google, propriété, version, déploiement, partage ou droit ACCESS n’a été modifié.

## 2. État figé

| Élément | État |
|---|---|
| Dépôt applicatif | `develop@d8ca2840659cad6b467772f2563ac61a834d6ada` |
| Project Book | `develop@8bc29c75bfa32c4b5f4af43a5ad6c83d7a5c8a26` |
| Backend Apps Script | `1_C157CtD95GegcpxSrKcL6bRDyea1cPfH9NfJywBnTmJQzKY30qfNNUu` |
| Backend code | un manifeste, aucune fonction métier |
| Support LOG | `AKS LOG RECETTE`, ID `1dQE7nwk2pAy9RADGZujleaRh-TvU5GfWVMnCYY1Hokk` |
| Secret | installé par D2-B, valeur non observée |
| Version du secret | `RECETTE-20260830-192805` |
| Transport portail | désactivé |
| Support de preuve | `AKS PRIVATE PROOFS RECETTE`, ID `10FS5Mpjnyt9iwi5ilaBK21p-QIPhSI8fBGwKBlf9dKA` |
| Propriétés backend | 11, dont `AKS_PRIVATE_ENABLED=false` |
| Version backend | aucune |
| Déploiement backend | aucun |

## 3. Inventaire du code intégré

Les composants privés suivants existent dans le dépôt applicatif :

| Fichier | Blob Git | Rôle | État D3 |
|---|---|---|---|
| `src/core/private/PrivateProtocol.gs` | `6edc7ab6fbee82c209f0fac11c97644481da905e` | protocole `AKS-PRIVATE/1`, HMAC, canonicalisation | réutilisable |
| `src/core/private/PrivateBackend.gs` | `096fcd3885b7ba3df7779d66a2379ba22373e327` | orchestration `LOG_READ_RECENT_V1` | réutilisable, ports non raccordés |
| `src/core/private/PrivateBackendAdapters.gs` | `571c2b654cfde48d67b0312ba69f892f31ca37fa` | adaptateurs injectables | uniquement abstrait |
| `src/core/private/PrivateReplayGuard.gs` | `2ccd0570aa9c8a276831304d96262a9c416bc331` | anti-rejeu `ScriptProperties + ScriptLock` | adaptateur Apps Script disponible |
| `src/core/private/PrivatePortalLogClient.gs` | `96570bd9156e249a6f83311b34e1c41483017b55` | client portail | inactif, hors package backend |
| `src/core/logging/LogEventRepository.gs` | `8dc6f5aabee80f8380780d4f786c9b2c6805943e` | dépôt LOG générique | défaut incompatible avec backend séparé |

Les suites existantes couvrent le protocole, l’orchestrateur, les ports injectés et l’anti-rejeu. La dernière validation cumulative connue a réussi à 716/716 dans Apps Script RECETTE.

## 4. Contrats déjà fermés

Le code impose déjà :

- protocole `AKS-PRIVATE/1` ;
- commande unique `LOG_READ_RECENT_V1` ;
- corps maximal de 16 384 octets ;
- fenêtre de requête maximale de 60 secondes ;
- tolérance d’horloge de 30 secondes ;
- limite de 1 à 20 événements ;
- environnement exact `RECETTE` ;
- projet appelant exact ;
- validation HMAC avant lecture LOG ;
- consommation anti-rejeu du `requestId` et du nonce ;
- preuve technique obligatoire après traitement ;
- réponses et événements minimisés ;
- refus fermé lorsque la configuration ou un port est indisponible.

Ces contrats ne suffisent pas à rendre le backend déployable.

## 5. Écarts bloquants

### 5.1 Aucun endpoint HTTP

Aucune fonction `doPost(e)` n’est exportée. Le backend ne peut recevoir aucune requête Web App.

L’endpoint D3-A devra :

1. accepter uniquement POST ;
2. extraire exclusivement `e.postData.contents` ;
3. refuser les requêtes sans corps ;
4. appeler l’orchestrateur privé ;
5. produire un JSON minimal ;
6. transformer toute erreur en réponse générique sans détail cryptographique ;
7. ne jamais journaliser le corps, la signature, le secret ou l’URL ;
8. n’exposer aucune autre commande.

### 5.2 Configuration réelle absente

L’adaptateur de configuration actuel reçoit un objet injecté. Il ne lit pas les propriétés Apps Script.

D3-A doit ajouter un adaptateur fermé lisant uniquement les clés contractuelles. Le secret courant et sa version sont déjà présents. Les clés non secrètes doivent être définies et contrôlées avant activation :

- activation explicite ;
- environnement `RECETTE` ;
- projet appelant exact ;
- identifiant du support LOG ;
- version backend ;
- paramètres anti-rejeu bornés ;
- identifiant du support de preuve.

Aucune clé ne doit utiliser une valeur par défaut permissive.

### 5.3 Lecteur LOG incompatible

`LogEventRepository` utilise par défaut `SpreadsheetApp.getActiveSpreadsheet()`. Un projet Apps Script autonome n’a pas de classeur actif.

Le backend doit ouvrir explicitement le support D1 par ID, vérifier :

- ID exact ;
- onglet exact `AKS_Logs` ;
- seize en-têtes exacts ;
- environnement RECETTE ;
- lecture seule ;
- limite stricte ;
- absence de retour des colonnes ou identifiants non autorisés.

Le support ne doit jamais être partagé aux comptes utilisateurs du portail.

### 5.4 Adaptateur de preuve absent

L’orchestrateur exige `proofWriter.write`, mais aucun support de preuve réel n’est raccordé.

Le support `AKS LOG RECETTE` contient actuellement un onglet unique et un schéma LOG exact. Il ne doit pas être réutilisé implicitement pour un schéma de preuve différent.

Avant déploiement, une décision distincte doit choisir :

- un support de preuve dédié ; ou
- un nouvel onglet dédié avec schéma fermé, après autorisation explicite.

Sans support de preuve persistant et contrôlé, D3 reste bloqué.

### 5.5 Package backend absent

Le dépôt ne contient pas de racine de package backend, de `.clasp.json` backend, ni de manifeste backend dédié. Le manifeste applicatif générique contient seulement :

- fuseau `Europe/Paris` ;
- runtime V8 ;
- journalisation Stackdriver ;
- aucune bibliothèque ;
- aucun service avancé.

Le package backend doit sélectionner seulement les fichiers nécessaires. Il ne doit pas copier les 261 fichiers du portail, ses interfaces, ses tests de production ou ses routes publiques.

## 6. Identité d’exécution et audience

### 6.1 Identité observée

Le backend et le support LOG D1 appartiennent au compte `karate.seremange@gmail.com`. Aucun compte technique distinct n’a été créé.

L’option techniquement cohérente à ce stade est un déploiement exécuté en tant que propriétaire du backend. Cette identité possède le support LOG RECETTE sans partage supplémentaire.

Cette option doit être explicitement acceptée avant D3 : le propriétaire associatif ferait alors fonction d’identité technique RECETTE. Elle ne préjuge pas de l’identité de PRODUCTION.

### 6.2 Audience

Le portail `USER_ACCESSING` doit appeler le backend côté serveur avec `UrlFetchApp`. Sans jeton OAuth backend partagé aux utilisateurs, l’audience techniquement compatible est une Web App accessible sans authentification Google, protégée applicativement par :

- HMAC ;
- environnement exact ;
- projet appelant exact ;
- fenêtre temporelle ;
- identifiants uniques ;
- anti-rejeu ;
- commande unique ;
- réponse minimisée.

Une audience `ANYONE` augmente la surface réseau. Elle n’est admissible en RECETTE qu’après revue explicite, tests de refus et confirmation que l’URL ne quitte jamais le serveur du portail.

Aucune audience n’est autorisée par ce précontrôle.

## 7. Propriétés non secrètes à contractualiser

Les noms définitifs doivent être implémentés et testés sur une branche applicative. Le contrat recommandé est :

| Clé | Projet | Valeur attendue |
|---|---|---|
| `AKS_PRIVATE_ENABLED` | backend | `false` avant test, puis `true` sous autorisation |
| `AKS_PRIVATE_ENVIRONMENT` | backend | `RECETTE` |
| `AKS_PRIVATE_CALLER_PROJECT` | backend | Script ID complet du portail RECETTE |
| `AKS_PRIVATE_LOG_SPREADSHEET_ID` | backend | ID exact du support D1 |
| `AKS_PRIVATE_PROOF_SPREADSHEET_ID` | backend | support à décider |
| `AKS_PRIVATE_BACKEND_VERSION` | backend | identifiant non secret de D3 |
| `AKS_PRIVATE_REPLAY_LOCK_WAIT_MS` | backend | entier borné |
| `AKS_PRIVATE_REPLAY_PURGE_LIMIT` | backend | entier borné |
| `AKS_PRIVATE_REPLAY_RETENTION_SKEW_MS` | backend | entier borné |

Les propriétés D2-B restent inchangées :

- `AKS_PRIVATE_HMAC_CURRENT` ;
- `AKS_PRIVATE_SECRET_VERSION` ;
- `AKS_PRIVATE_HMAC_PREVIOUS` absente.

Le portail ne reçoit aucune propriété D3. L’URL et l’activation du client relèvent de D4.

## 8. Package backend D3-A

Le package minimal devra contenir :

1. protocole privé ;
2. orchestrateur backend ;
3. adaptateurs backend ;
4. garde anti-rejeu ;
5. adaptateur de configuration Apps Script ;
6. lecteur LOG dédié par ID ;
7. écrivain de preuve dédié ;
8. endpoint `doPost` fermé ;
9. manifeste V8 Europe/Paris ;
10. tests backend dédiés exécutables avant publication.

Le package doit exclure :

- `PrivatePortalLogClient.gs` ;
- les contrôleurs et interfaces du portail ;
- ACCESS côté portail ;
- les modules métier sans dépendance backend ;
- tout identifiant ou secret codé en dur ;
- tout fichier de test dans la version publiée, sauf décision explicite contraire.

## 9. Tests requis avant tout déploiement

D3-A doit ajouter des tests pour :

- configuration absente ou désactivée ;
- propriété inconnue ou valeur hors bornes ;
- projet appelant erroné ;
- environnement autre que RECETTE ;
- secret absent et clé précédente absente ;
- POST absent, vide, trop grand ou invalide ;
- méthode ou commande interdite ;
- signature, payload ou réponse altérés ;
- expiration et dérive d’horloge ;
- rejeu de `requestId` et nonce ;
- verrou indisponible ;
- support LOG absent, mauvais onglet ou mauvais schéma ;
- support de preuve indisponible ;
- fuite de données dans réponses et journaux ;
- limitation à vingt événements ;
- réponse d’erreur générique ;
- absence de route supplémentaire ;
- package backend sans dépendance portail.

La suite cumulative applicative doit rester intégralement verte. Une validation réversible dans le backend RECETTE doit précéder toute version.

## 10. Séquence D3 recommandée

### D3-A — implémentation applicative

Sur branche dédiée :

- endpoint fermé ;
- adaptateurs réels mais désactivés par défaut ;
- package backend reproductible ;
- manifeste dédié ;
- tests ;
- PR vers `develop`.

Aucune opération Google.

### D3-B — support de preuve et propriétés non secrètes

Après décision séparée :

- créer ou préparer le support de preuve RECETTE ;
- installer les propriétés non secrètes ;
- maintenir `AKS_PRIVATE_ENABLED=false` ;
- vérifier noms et empreintes ;
- aucun déploiement.

### D3-C — validation réversible du HEAD backend

- sauvegarde exacte ;
- push temporaire du package ;
- tests directs sans endpoint public ;
- relecture ;
- restauration exacte ;
- aucune version ni déploiement.

### D3-D — version et déploiement backend

Après validation :

- push du package validé ;
- relecture intégrale ;
- version Apps Script dédiée ;
- déploiement Web App avec identité et audience explicitement autorisées ;
- tests de refus et requête signée contrôlée ;
- portail toujours non raccordé ;
- retour arrière du seul déploiement backend.

Chaque sous-lot exige une autorisation séparée.

## 11. Retour arrière

Avant D3-C ou D3-D, figer :

- HEAD backend ;
- manifeste ;
- propriétés par noms et empreintes ;
- versions et déploiements ;
- support LOG et support de preuve ;
- partages ;
- empreintes des archives locales.

Le retour arrière doit :

1. maintenir ou remettre le portail non raccordé ;
2. désactiver le backend ;
3. restaurer le déploiement backend ou le supprimer s’il a été créé par D3 ;
4. restaurer le HEAD backend ;
5. supprimer seulement les propriétés non secrètes créées par D3 ;
6. conserver les deux propriétés D2-B sauf autorisation explicite de retrait ;
7. supprimer seulement les entrées anti-rejeu créées par les tests ;
8. restaurer le support de preuve ;
9. vérifier l’absence de partage utilisateur ;
10. relire tous les états.

## 12. Décision après D3-A

D3-A est terminé et intégré dans `develop`. Les écarts applicatifs identifiés par le précontrôle sont désormais couverts :

- point d’entrée `doPost` dédié, conservé hors du package portail ;
- configuration stricte par propriétés, sans valeur permissive ;
- lecteur LOG dédié par identifiant et schéma exact ;
- écrivain de preuve dédié, verrouillé et relu ;
- erreurs HTTP génériques sans détail sensible ;
- package backend reproductible à liste blanche de sept fichiers ;
- manifeste backend sans décision prématurée d’audience ou d’identité ;
- backend inactif tant que `AKS_PRIVATE_ENABLED` n’est pas exactement `true`.

D3-C est désormais terminé. Le package backend a été validé réversiblement avec `AKS_PRIVATE_ENABLED=false`, puis le HEAD initial a été restauré exactement. D3-D reste soumis à une autorisation distincte portant explicitement sur le push persistant, la version, l’identité d’exécution, l’audience et le déploiement.

## 13. Résultat d’implémentation et de validation D3-A

La PR applicative #143, tête `e2d21b492167a2a0571035dcff66230819a0d7c1`, a été fusionnée dans `develop` au commit `bc8b3be19c3860079db0228769d8cb1d80f302dc`.

La validation a établi :

| Contrôle | Résultat |
|---|---|
| Tests D3-A locaux | 13/13 |
| Suite cumulative Apps Script RECETTE | 729/729 |
| Candidate relue | 271 fichiers |
| HEAD initial | 261 fichiers |
| Restauration | 261 fichiers, exacte |
| Archive de sauvegarde SHA-256 | `26BB92891E32FA839C55ED2E360835188A74CE94C0C2B6AA80C7955660A4D2F3` |
| Version ou déploiement | aucun créé ou modifié |

Le package backend, son point d’entrée et son manifeste sont hors de `src` et n’ont pas été poussés vers le portail. Le runtime ajouté au portail reste fermé faute de propriété d’activation. Aucun projet backend, support Google, secret, propriété, partage ou droit ACCESS n’a été modifié.

## 14. Résultat D3-B

D3-B a été exécuté exclusivement en RECETTE.

### 14.1 Support de preuve

Le classeur natif Google Sheets `AKS PRIVATE PROOFS RECETTE` a été créé sous l’identité propriétaire observée, sans partage. Il contient un seul onglet `AKS_Private_Proofs`, aucune ligne de preuve et les dix en-têtes contractuels :

- `environment` ;
- `command` ;
- `actorHash` ;
- `requestId` ;
- `correlationId` ;
- `result` ;
- `durationMs` ;
- `returnedCount` ;
- `backendVersion` ;
- `recordedAt`.

Son identifiant est `10FS5Mpjnyt9iwi5ilaBK21p-QIPhSI8fBGwKBlf9dKA`.

### 14.2 Propriétés backend

Le backend privé RECETTE porte exactement onze propriétés :

| Propriété | Valeur non secrète ou état |
|---|---|
| `AKS_PRIVATE_ENABLED` | `false` |
| `AKS_PRIVATE_ENVIRONMENT` | `RECETTE` |
| `AKS_PRIVATE_CALLER_PROJECT` | portail RECETTE exact |
| `AKS_PRIVATE_LOG_SPREADSHEET_ID` | support LOG D1 exact |
| `AKS_PRIVATE_PROOF_SPREADSHEET_ID` | support de preuve D3-B exact |
| `AKS_PRIVATE_BACKEND_VERSION` | `ADMIN-006-D3A` |
| `AKS_PRIVATE_REPLAY_LOCK_WAIT_MS` | `5000` |
| `AKS_PRIVATE_REPLAY_PURGE_LIMIT` | `50` |
| `AKS_PRIVATE_REPLAY_RETENTION_SKEW_MS` | `30000` |
| `AKS_PRIVATE_HMAC_CURRENT` | présente, empreinte D2-B inchangée |
| `AKS_PRIVATE_SECRET_VERSION` | `RECETTE-20260830-192805` |

`AKS_PRIVATE_HMAC_PREVIOUS` reste absente. Aucune valeur secrète n’a été affichée ou enregistrée.

### 14.3 Restauration du backend

La sauvegarde préalable porte l’empreinte SHA-256 `32CD60068CDBA2D68A0D34DBA4A3161E28BEC3B3A77A4E0E8E1D31E7837DB667`.

Le premier push de restauration n’a pas supprimé le fichier temporaire `Admin006D3BInstaller.gs`, conformément à la limite déjà observée de `clasp push --force`. La récupération fermée a consisté à supprimer uniquement ce fichier dans l’éditeur puis à relire intégralement le backend.

Le contrôle final confirme :

- un fichier attendu et un fichier observé ;
- installateur absent ;
- manifeste identique à la sauvegarde ;
- backend restauré exactement ;
- `AKS_PRIVATE_ENABLED=false`.

### 14.4 Décision

D3-B est conforme. Aucun code métier backend, version, déploiement, partage, droit ACCESS, élément de production ou élément INSCRIPTIONS-011 n’a été créé ou modifié.

D3-C peut être autorisé séparément pour le push temporaire du package backend D3-A, sa validation directe et la restauration exacte du HEAD, sans version ni déploiement.

## 15. Résultat D3-C

D3-C a validé réversiblement le package backend construit exclusivement depuis `develop@bc8b3be19c3860079db0228769d8cb1d80f302dc`.

| Contrôle | Résultat |
|---|---|
| Fichiers du package métier | 7 |
| Relecture après push | zéro différence |
| Configuration | `AKS_PRIVATE_ENABLED=false` |
| Réponse fermée | validée |
| Propriétés | onze, inchangées |
| Secret et version | inchangés |
| `AKS_PRIVATE_HMAC_PREVIOUS` | absente |
| Restauration du HEAD | exacte, zéro différence |
| Versions Apps Script | inchangées |
| Déploiements Apps Script | inchangés |
| Archive de sauvegarde SHA-256 | `0F94658D368B1EF3824D34A9577513CCD00255DFF20EA095C4DFD2218D7ED5C0` |

L’instrumentation temporaire et les six fichiers métier ont été supprimés visiblement dans l’éditeur, puis le backend a été relu. Le contrôle final observe uniquement le manifeste initial.

Aucune propriété, version, déploiement, audience, partage, habilitation ACCESS, ressource de production ou opération INSCRIPTIONS-011 n’a été créé ou modifié.

D3-D peut être cadré séparément. Il devra figer l’identité d’exécution et l’audience, pousser le package validé, relire les sept fichiers, créer une version puis un unique déploiement backend avec retour arrière explicite. Le portail restera non raccordé et le backend désactivé tant qu’une autorisation ultérieure ne prévoit pas son activation.

## 16. Cadrage D3-D

Le cadrage détaillé est consigné dans [ADMIN-006-14](ADMIN-006-14.md).

Il retient en RECETTE :

- `USER_DEPLOYING` pour l’identité d’exécution ;
- `ANYONE_ANONYMOUS` pour l’audience compatible avec l’appel serveur à serveur ;
- une section `webapp` ajoutée au manifeste uniquement par une PR applicative ;
- une validation réversible du manifeste modifié ;
- une publication backend maintenue inactive ;
- un test signé temporaire et réversible distinct ;
- aucun raccordement fonctionnel du portail avant D4.

La prochaine étape est D3-D1, limitée au manifeste, aux tests et à une PR applicative. Aucune opération Google n’est autorisée par le cadrage.

## 17. Historique

| Version | Date | Évolution |
|---|---|---|
| 0.5.0 | 2026-08-31 | D3-D cadré dans ADMIN-006-14 : identité, audience, manifeste, publication inactive, preuve signée et retours arrière séparés |
| 0.4.0 | 2026-08-30 | D3-C conforme : package de sept fichiers relu sans écart, fonctionnement désactivé validé, HEAD restauré exactement, versions et déploiements inchangés |
| 0.3.0 | 2026-08-30 | D3-B conforme : support de preuve dédié non partagé, onze propriétés backend avec activation à false, secret inchangé et backend restauré exactement |
| 0.2.0 | 2026-08-30 | D3-A intégré : runtime fermé, package backend dédié, 13/13 puis 729/729, restauration RECETTE exacte, aucune opération Google |
| 0.1.0 | 2026-08-30 | Précontrôle D3 : code privé inventorié, sept écarts bloquants identifiés et séquence D3-A à D3-D définie sans opération Google |
