# ADMIN-006-14 — Cadrage D3-D du backend privé RECETTE

| Propriété | Valeur |
|---|---|
| **Document ID** | ADMIN-006-14 |
| **Titre** | Manifeste, version et premier déploiement du backend privé |
| **Version** | 0.3.0 |
| **Statut** | D3-D3 conforme — D3-D4 soumis à autorisation distincte |
| **Nature** | Architecture de déploiement, sécurité, tests et retour arrière |
| **Propriétaire** | Product Owner |
| **Dernière mise à jour** | 2026-08-31 |
| **Dépendances** | ADMIN-006, ADMIN-006-08, ADMIN-006-12, ADMIN-006-13, SECURITY-001, LOG-001 |
| **Référence applicative** | `develop@064ca709a754915a87451e829c24088ec878fdde` |
| **Référence Project Book** | `develop@d75fae64122e92a23fe2a901958c4a48beac2a62` |
| **Environnement** | RECETTE uniquement |

---

## 1. Objet

D3-D prépare le premier déploiement Web App du backend privé RECETTE après la validation réversible D3-C. Ce document fixe l’identité d’exécution, l’audience, le manifeste, la séquence de publication, les tests de refus, la preuve signée et le retour arrière.

Ce cadrage ne modifie aucun dépôt, projet Apps Script, manifeste, secret, propriété, support Google, version, déploiement, partage, droit ACCESS ou ressource de production.

## 2. État d’entrée figé

| Élément | État |
|---|---|
| Backend Apps Script | `1_C157CtD95GegcpxSrKcL6bRDyea1cPfH9NfJywBnTmJQzKY30qfNNUu` |
| HEAD backend | un manifeste, aucun code métier |
| Propriétés backend | 11 exactement |
| Activation | `AKS_PRIVATE_ENABLED=false` |
| Secret courant | installé, empreinte D2-B inchangée |
| Version du secret | `RECETTE-20260830-192805` |
| Clé précédente | absente |
| Support LOG | `AKS LOG RECETTE`, non partagé |
| Support de preuve | `AKS PRIVATE PROOFS RECETTE`, non partagé et vide |
| Package D3-C | 7 fichiers, relecture sans différence |
| Version backend | aucune |
| Déploiement backend | aucun |
| Portail | non raccordé |

L’archive D3-C porte l’empreinte SHA-256 `0F94658D368B1EF3824D34A9577513CCD00255DFF20EA095C4DFD2218D7ED5C0`.

## 3. Décision d’identité d’exécution

Le backend doit s’exécuter sous l’identité du compte qui réalise son déploiement, soit `USER_DEPLOYING`.

En RECETTE, le déploiement doit être réalisé exclusivement par le propriétaire observé `karate.seremange@gmail.com`, qui possède déjà le projet backend, le support LOG et le support de preuve. Aucun partage utilisateur supplémentaire n’est requis.

Cette décision :

- isole les permissions Google des comptes qui utilisent le portail ;
- évite le défaut `USER_ACCESSING` à l’origine d’ADMIN-006 ;
- ne crée pas de compte technique distinct ;
- ne vaut pas décision d’identité pour la PRODUCTION ;
- impose de contrôler à nouveau le propriétaire avant le déploiement.

## 4. Décision d’audience

Le portail appelle le backend côté serveur par `UrlFetchApp` sans cookie de session Google ni jeton OAuth transmis. L’audience compatible est donc `ANYONE_ANONYMOUS`.

Le manifeste cible devra contenir :

```json
"webapp": {
  "access": "ANYONE_ANONYMOUS",
  "executeAs": "USER_DEPLOYING"
}
```

Selon la documentation Apps Script, `ANYONE_ANONYMOUS` autorise l’invocation sans connexion Google et `USER_DEPLOYING` exécute la Web App sous l’identité du déployeur. Cette audience augmente la surface réseau ; elle n’est admissible que parce que le backend applique cumulativement :

- commande unique `LOG_READ_RECENT_V1` ;
- protocole exact `AKS-PRIVATE/1` ;
- HMAC ;
- fenêtre temporelle ;
- projet appelant et environnement exacts ;
- `requestId` et nonce uniques ;
- registre anti-rejeu ;
- corps et résultat bornés ;
- réponses d’erreur génériques ;
- absence de secret et d’URL dans les journaux ;
- backend désactivé par défaut.

Références officielles :

- https://developers.google.com/apps-script/manifest/web-app-api-executable
- https://developers.google.com/apps-script/guides/web

## 5. Modification applicative préalable obligatoire

Le manifeste D3-A validé ne contient volontairement aucune section `webapp`. Le déploiement ne doit pas utiliser une modification locale non revue.

D3-D est donc découpé en quatre sous-lots. Le premier est obligatoirement applicatif.

### D3-D1 — contrat de manifeste

Sur une branche applicative dédiée :

1. ajouter exactement la section `webapp` décidée ci-dessus au manifeste backend ;
2. conserver V8, `Europe/Paris`, les dépendances vides et Stackdriver ;
3. adapter les tests du constructeur de package ;
4. vérifier que le manifeste du portail reste inchangé ;
5. exécuter les tests dédiés et la suite cumulative ;
6. ouvrir une PR vers `develop`.

Aucune opération Google n’est incluse.

### D3-D2 — validation réversible du manifeste

Après intégration de D3-D1 :

1. reconstruire le package depuis le commit fusionné ;
2. sauvegarder exactement le HEAD backend ;
3. pousser temporairement les sept fichiers ;
4. relire les sept fichiers et le manifeste ;
5. vérifier le refus fermé avec `AKS_PRIVATE_ENABLED=false` ;
6. supprimer visiblement les six fichiers `.gs` ;
7. relire le backend et confirmer le retour exact au manifeste initial.

Aucune version ni aucun déploiement n’est créé.

### D3-D3 — publication inactive

Après validation D3-D2 :

1. sauvegarder HEAD, propriétés, versions, déploiements et partages ;
2. pousser le package validé ;
3. relire les sept fichiers sans différence ;
4. confirmer les onze propriétés et `AKS_PRIVATE_ENABLED=false` ;
5. créer une seule version Apps Script avec une description déterministe ;
6. vérifier le numéro exact retourné ;
7. créer un seul déploiement Web App sur cette version ;
8. vérifier l’identité `USER_DEPLOYING`, l’audience `ANYONE_ANONYMOUS` et l’URL `/exec` ;
9. envoyer des requêtes invalides et confirmer uniquement `PRIVATE_BACKEND_UNAVAILABLE` ;
10. confirmer zéro ligne LOG et zéro ligne de preuve ajoutée ;
11. conserver le portail non raccordé.

Le backend publié reste inactif.

### D3-D4 — preuve signée contrôlée

Sous une nouvelle autorisation :

1. installer temporairement l’URL backend côté serveur dans un harnais contrôlé du portail ;
2. sauvegarder les propriétés des deux projets ;
3. activer temporairement uniquement le backend ;
4. émettre une requête signée `LOG_READ_RECENT_V1` ;
5. vérifier la réponse minimisée ;
6. vérifier une seule preuve persistante ;
7. vérifier la consommation anti-rejeu ;
8. rejouer la même requête et confirmer son refus ;
9. remettre `AKS_PRIVATE_ENABLED=false` ;
10. retirer le harnais et l’URL temporaire ;
11. restaurer et relire exactement le portail ;
12. conserver le déploiement backend mais inactif.

D3-D4 ne constitue pas le raccordement fonctionnel D4 du portail.

## 6. Précontrôles fermés

Chaque sous-lot s’arrête avant mutation si :

- la référence applicative attendue a changé ;
- le HEAD backend n’est pas celui attendu ;
- les onze propriétés ne sont pas exactes ;
- le secret ou sa version a changé ;
- `AKS_PRIVATE_HMAC_PREVIOUS` est présente ;
- `AKS_PRIVATE_ENABLED` n’est pas `false` au départ ;
- un support est partagé à un compte utilisateur ;
- le schéma LOG ou preuve a changé ;
- une version ou un déploiement backend existe avant D3-D3 ;
- le propriétaire ou le compte déployeur n’est pas celui attendu ;
- le manifeste contient une autre audience ou identité ;
- le package ne contient pas exactement sept fichiers ;
- le portail contient déjà une URL backend ou un transport actif.

## 7. Tests de refus avant activation

Le déploiement inactif doit retourner une erreur générique pour :

- GET ou absence de POST valide ;
- corps vide ;
- JSON invalide ;
- protocole ou commande inconnus ;
- signature absente, invalide ou altérée ;
- environnement ou projet appelant incorrect ;
- requête expirée ou située dans le futur ;
- identifiants absents ou mal formés ;
- corps trop volumineux.

Aucun test de refus ne doit :

- ouvrir le support LOG ;
- écrire une preuve ;
- créer une clé anti-rejeu ;
- afficher la signature, le secret ou l’URL dans un rapport public ;
- modifier le portail.

## 8. Test signé contrôlé

Le test signé ne peut avoir lieu qu’après D3-D3 et sous autorisation D3-D4. Il utilise le secret déjà installé dans les propriétés du portail sans l’extraire ni l’afficher.

La preuve attendue est exactement une nouvelle ligne comprenant le hash de l’acteur et les identifiants techniques, jamais son adresse en clair. Le support LOG reste en lecture seule.

Le test est conforme uniquement si :

- la première requête réussit ;
- au plus vingt événements minimisés sont retournés ;
- une seule preuve est écrite puis relue ;
- le rejeu exact est refusé ;
- aucune seconde preuve de succès n’est créée ;
- aucune donnée LOG n’est modifiée ;
- aucun support n’est partagé ;
- le backend est remis à `false` immédiatement après le test.

## 9. Retour arrière

### 9.1 Avant création du déploiement

Restaurer le HEAD backend initial, retirer manuellement tout fichier distant supplémentaire, relire un manifeste unique et confirmer propriétés, versions et déploiements inchangés.

### 9.2 Après création de version mais avant déploiement

Restaurer le HEAD. La version Apps Script immuable reste consignée comme version non déployée. Aucune tentative de suppression ou de réécriture n’est autorisée.

### 9.3 Après création du premier déploiement

1. remettre `AKS_PRIVATE_ENABLED=false` ;
2. supprimer exclusivement le déploiement créé par D3-D3 ou le ramener à une version inactive explicitement connue ;
3. confirmer l’absence d’autre déploiement ;
4. restaurer le HEAD backend initial si la publication est annulée ;
5. conserver les secrets D2-B sauf autorisation distincte ;
6. conserver les propriétés D3-B sauf autorisation distincte ;
7. supprimer uniquement les entrées anti-rejeu et preuves créées par les tests ;
8. vérifier zéro modification LOG ;
9. vérifier le portail non raccordé ;
10. produire un rapport sans secret.

## 10. Rapports autorisés

Les rapports peuvent contenir :

- identifiants de projet, version et déploiement ;
- URL backend uniquement dans un rapport local protégé, pas dans Git ;
- identité et audience ;
- noms et empreintes de propriétés ;
- nombres de fichiers et différences ;
- codes de réponse génériques ;
- nombres de lignes LOG, preuve et anti-rejeu ;
- empreintes des archives ;
- résultats de restauration.

Ils ne contiennent jamais le secret, une signature complète, un corps signé, un jeton OAuth ou des données LOG métier.

## 11. Critères d’acceptation du cadrage

Le cadrage est conforme car :

- l’identité et l’audience sont décidées ;
- le manifeste exact est défini ;
- la modification applicative préalable est séparée ;
- validation, publication et test signé sont découplés ;
- le backend reste inactif pendant la publication ;
- le portail reste non raccordé ;
- le test signé est réversible ;
- le retour arrière couvre chaque point d’irréversibilité ;
- aucune ressource de PRODUCTION ou INSCRIPTIONS-011 n’est incluse.

D3-D1, D3-D2 et D3-D3 sont terminés. La prochaine étape recommandée est D3-D4 uniquement, sous une autorisation distincte.

## 12. Résultats D3-D1 et D3-D2

### 12.1 D3-D1 — contrat applicatif du manifeste

La PR applicative #144, tête `99d830e748e88b1ed08f2039a09f8c1fd1afe9e7`, a été fusionnée dans `develop` au commit `064ca709a754915a87451e829c24088ec878fdde`.

Elle a ajouté au package backend uniquement le contrat Web App `ANYONE_ANONYMOUS` / `USER_DEPLOYING`, renforcé le constructeur de package et conservé un package minimal de sept fichiers. Aucun fichier du portail, projet Apps Script ou support Google n’a été modifié par D3-D1.

### 12.2 D3-D2 — validation réversible RECETTE

D3-D2 a validé le package construit exclusivement depuis le commit applicatif fusionné.

| Contrôle | Résultat |
|---|---|
| Fichiers du package | 7 |
| Relecture après push | zéro différence |
| Validation fermée | réussie |
| `AKS_PRIVATE_ENABLED` | `false` |
| Versions Apps Script | inchangées |
| Déploiements Apps Script | inchangés |
| Archive de sauvegarde SHA-256 | `1B2CE18C70A3D7AC216FFBA5AF8BFDEE5EC346C88B48DAF26B746680E60B7955` |
| Restauration finale | exacte |

Après la suppression visible des fichiers temporaires et métier, Apps Script a conservé la section `webapp` malgré le push du manifeste initial. L’écart était limité à `appsscript.json`. La section `webapp` a été retirée manuellement dans l’éditeur, puis le HEAD a été relu : l’empreinte du manifeste observé est redevenue exactement celle de la sauvegarde initiale.

Aucune propriété, version, déploiement, partage, habilitation ACCESS, ressource de production ou opération INSCRIPTIONS-011 n’a été créé ou modifié. D3-D3 peut être autorisé séparément pour la publication persistante du backend maintenu inactif.

## 13. Résultat D3-D3 — publication inactive

D3-D3 a publié durablement le backend privé RECETTE depuis `develop@064ca709a754915a87451e829c24088ec878fdde`.

| Contrôle | Résultat |
|---|---|
| Fichiers publiés | 7 |
| Relecture du HEAD | zéro différence |
| Version Apps Script | 1 |
| Déploiement Web App | unique, associé à la version 1 |
| Audience | `ANYONE_ANONYMOUS` |
| Identité d’exécution | `USER_DEPLOYING` |
| `AKS_PRIVATE_ENABLED` | `false` |
| Réponse fermée | `PRIVATE_BACKEND_UNAVAILABLE`, validée |
| Portail | non raccordé |
| Retour arrière | non requis |
| Sauvegarde SHA-256 | `AAEC5505B5E1D22E57CE7F130F1A7836C01BCD4FB4A2E05721796067D2B3EAAC` |

L’identifiant et l’URL du déploiement restent exclusivement dans le rapport local protégé et ne sont pas publiés dans Git. Aucun secret n’a été affiché ou enregistré. Aucune propriété, partage, habilitation ACCESS, ressource de production ou opération INSCRIPTIONS-011 n’a été modifié.

Le backend est publié mais demeure inactif. D3-D4 doit faire l’objet d’une autorisation séparée pour une activation temporaire, une requête signée contrôlée, la preuve persistante unique, le test anti-rejeu puis le retour immédiat à `false`.

## 14. Historique

| Version | Date | Évolution |
|---|---|---|
| 0.3.0 | 2026-08-31 | D3-D3 conforme : version 1 et déploiement Web App unique, backend maintenu inactif, réponse fermée validée, portail non raccordé |
| 0.2.0 | 2026-08-31 | D3-D1 intégré et D3-D2 validé réversiblement ; manifeste initial restauré exactement après retrait manuel de la section webapp conservée par Apps Script |
| 0.1.0 | 2026-08-31 | Cadrage D3-D : USER_DEPLOYING, ANYONE_ANONYMOUS, manifeste revu, validation, publication inactive, preuve signée et retours arrière séparés |
