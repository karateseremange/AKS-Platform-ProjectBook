# ADMIN-006-03 — Résultat du lot A des contrats privés

| Propriété | Valeur |
|---|---|
| **Document ID** | ADMIN-006-03 |
| **Titre** | Implémentation des contrats purs AKS-PRIVATE/1 |
| **Version** | 0.2.0 |
| **Statut** | Validé en RECETTE — fusion applicative attendue |
| **Nature** | Rapport d’implémentation et de validation |
| **Propriétaire** | Product Owner |
| **Dernière mise à jour** | 2026-08-28 |
| **Dépendances** | ADMIN-006, ADMIN-006-01, ADMIN-006-02 |
| **PR applicative** | AKS-Platform #140 |
| **Tête applicative** | `0fab43539a8260f4fbd29113efe486d58a65cc22` |

---

## 1. Objet

Ce document consigne l’exécution du lot A défini par ADMIN-006-02. Le lot construit uniquement les contrats purs nécessaires au futur canal privé. Il ne raccorde aucun backend et n’accède à aucune ressource Google.

## 2. Références

- base applicative : `develop@80b33ec648850a2b58bedda0c7ee10fc68cebf91` ;
- branche : `feat/admin-006-lot-a-private-protocol` ;
- PR : https://github.com/karateseremange/AKS-Platform/pull/140 ;
- tête : `0fab43539a8260f4fbd29113efe486d58a65cc22` ;
- fichiers modifiés : 3 ;
- écart : 883 ajouts, aucune suppression.

## 3. Livrables applicatifs

| Fichier | Rôle |
|---|---|
| `src/core/private/PrivateProtocol.gs` | contrats, canonicalisation, signatures, validations et adaptateur cryptographique |
| `src/tests/admin/Admin006PrivateProtocolTest.gs` | 18 tests unitaires du lot A |
| `src/tests/suites/TestSuiteV11.gs` | rattachement des 18 tests à la suite cumulative |

## 4. Contrat implémenté

Le composant expose :

- protocole `AKS-PRIVATE/1` ;
- commande unique `LOG_READ_RECENT_V1` ;
- limite de 20 événements ;
- corps maximal de 16 384 caractères au stade du lot A ;
- fenêtre maximale de 60 secondes ;
- tolérance d’horloge de 30 secondes ;
- niveaux autorisés `DEBUG`, `INFO`, `WARN`, `ERROR`, `CRITICAL` ;
- canonicalisation déterministe des objets JSON ;
- empreinte SHA-256 du payload ;
- signature HMAC-SHA256 encodée en Base64 ;
- comparaison de signature à travail constant ;
- enveloppes signées de requête et de réponse ;
- contrôle de corrélation des réponses ;
- interface injectée de consommation anti-rejeu.

## 5. Propriétés de sécurité

### 5.1 Catalogue fermé

Le payload n’accepte que `limit`, `severity` et `cursor`. Tout identifiant de classeur, URL Drive, filtre libre ou champ inconnu est refusé.

### 5.2 Identité

L’acteur est normalisé en adresse électronique en minuscules. Le lot A ne résout pas lui-même l’utilisateur actif : cette responsabilité reste celle du futur adaptateur portail.

### 5.3 Temps

Une requête est refusée si les dates ne sont pas ISO UTC canoniques, si l’expiration précède l’émission, si la fenêtre dépasse 60 secondes, si la requête est expirée ou si l’émission dépasse la tolérance d’horloge.

### 5.4 Anti-rejeu

Le contrat exige une dépendance `replayGuard.consume(requestId, nonce, expiresAtMs)`. Aucun stockage réel n’est fourni dans le lot A. L’absence de garde ou le rejet de la consommation ferme l’opération.

### 5.5 Réponses

La réponse est liée à l’environnement, à l’identifiant de requête, à l’identifiant de corrélation, à son empreinte de données et à sa signature. Une réponse altérée ou non corrélée est ramenée à `PRIVATE_BACKEND_UNAVAILABLE`.

## 6. Absence d’opération Google

Le contrôle statique confirme l’absence de `SpreadsheetApp`, `DriveApp`, `PropertiesService`, `UrlFetchApp`, `CacheService` et `LockService` dans les trois fichiers du lot.

L’adaptateur cryptographique utilise uniquement `Utilities` pour SHA-256, HMAC-SHA256 et Base64. Il ne réalise aucune entrée-sortie.

## 7. Tests

### 7.1 Résultat du lot A

Les 18 tests dédiés ont été exécutés dans un harnais local compatible avec les primitives Apps Script nécessaires :

| Résultat | Valeur |
|---|---:|
| Total | 18 |
| Réussis | 18 |
| Échecs | 0 |

Ils couvrent le contrat fermé, la canonicalisation, les valeurs interdites, l’acteur, le payload, les signatures, les altérations, l’environnement, l’expiration, le rejeu, les vecteurs cryptographiques et les réponses corrélées.

### 7.2 Vecteurs cryptographiques

- SHA-256 de `abc` :
  `ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad`
- HMAC-SHA256 Base64 du message de référence avec la clé `key` :
  `97yD9DBThCSxMpjmqm+xQ+9NWaFJRhdZl0edvC0aPNg=`

### 7.3 Suite cumulative

La suite cumulative `AKS_runValidationSuiteV11` contient exactement 683 cas, dont les 18 nouveaux cas ADMIN-006. Le fichier de suite contient un objet de test nommé supplémentaire appartenant à une suite auxiliaire ; il ne fait pas partie du tableau cumulatif.

La tête applicative `0fab43539a8260f4fbd29113efe486d58a65cc22` a été poussée temporairement vers le projet RECETTE suffixé `eIRxs4`, relue intégralement à 263 fichiers puis exécutée depuis l’éditeur Apps Script. Résultat : **683/683 réussis, zéro échec**.

Après validation, RECETTE a été restaurée et relue à 261/261 fichiers. L’archive préalable porte l’empreinte SHA-256 `032B7DC760D7AFD12D9B2A7A5590625A7D5BCCBD83607F047D0D0CDEE552EB54`. Aucune version ni aucun déploiement n’a été créé ou modifié.

## 8. Limites connues

Le lot A ne fournit pas de client portail, endpoint backend, stockage persistant du nonce, verrouillage concurrent, secret, propriétés d’environnement, lecture des journaux, résilience du widget Administration ou preuve AUDIT réelle.

La limite de corps est actuellement contrôlée sur la longueur canonique JavaScript. Le lot backend devra confirmer ou renforcer la mesure exacte en octets UTF-8 au point d’entrée HTTP.

## 9. Contrôles avant fusion applicative

Avant de fusionner la PR #140 :

1. confirmer que sa tête reste `0fab43539a8260f4fbd29113efe486d58a65cc22` ;
2. vérifier qu’elle n’est pas en retard sur `develop` ;
3. revoir les trois fichiers ;
4. vérifier le rapport RECETTE concluant à 683/683 et la restauration exacte ;
5. ne créer ni secret, ni backend, ni partage Google.

## 10. Suite proposée

Après validation et fusion séparées :

1. concevoir le stockage anti-rejeu persistant et borné ;
2. implémenter le backend RECETTE sans le déployer ;
3. isoler l’échec LOG dans le portail ;
4. préparer seulement ensuite une opération Apps Script RECETTE contrôlée.

Chaque étape nécessite une nouvelle autorisation.

## 11. Historique

| Version | Date | Évolution |
|---|---|---|
| 0.2.0 | 2026-08-29 | Validation réversible en RECETTE : candidate relue à 263 fichiers, suite cumulative 683/683 réussie, restauration exacte à 261 fichiers, aucun déploiement modifié |
| 0.1.0 | 2026-08-28 | Résultat du lot A : contrats purs AKS-PRIVATE/1, 18/18 tests isolés réussis, suite cumulative étendue à 684 cas, aucune opération Google |

